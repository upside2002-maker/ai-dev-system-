# Sitka Operating

Живой dashboard оперативного состояния sitka-office. Обновляется TL или Admin **по событию** (новый TASK, новый HANDOFF, изменение Reviewer findings).

Соседние артефакты:

- `OPERATING/journal/<YYYY-MM>.md` — журнал событий по датам.
- `OPERATING/backlog.md` — открытые мелочи (mini-tasks, drive-by, drafts).
- `STATUS_RU.md` — пользовательский статус без английской терминологии.
- `CURRENT_STATE.md` — snapshot фазы и архитектуры.

## Активные TASKS

- [`TASKS/2026-04-29-dm7-c-backend-widget-prereq.md`](TASKS/2026-04-29-dm7-c-backend-widget-prereq.md) — `Status: open` → DRAFT → Worker → расширить `Api.TransactionResp` полем `trExpenseCategoryId :: Maybe Int64`. **НЕ срочно** — запускать только когда оператор захочет breakdown в виджете. Класс A без `Mode:` — перед запуском нужно дописать `Mode: strict` (или явно понизить класс с обоснованием), иначе `accept-task` откажет; см. STATUS_RU.md.

См. [`TASKS/README.md`](TASKS/README.md) для правил.

## Открытые HANDOFFS

(нет)

См. [`HANDOFFS/README.md`](HANDOFFS/README.md) для правил.

## Reviewer findings ожидающие TL

(нет)

## Активные Worker сессии

(нет)

## Заметки

- 2026-05-28 (ночь): **TASK B (P0-1 substring drop) закрыт.** PR #91 → master `89a6c25`. Worker iter1 (one commit, drive-by unused-import removal + GTX special-case удалён, обоснование через `_TOKEN_EQUIVALENTS["gtx"]`) → Reviewer iter1 ACCEPT (no blockers, 3 non-blocking nits). `title_matches` упрощён до `len(matched_tokens) == len(search_tokens)`. Hypothesis property-тесты: soundness + completeness × 200 examples. **Prod smoke:** «blizzard aerolite parka» → 1shot «Blizzard AeroLite Parka» $538.30 (был бы 0 до fix'а; substring между токенами «Bib» блокировал). **Параллельно — recurring CORE_AUTH drift root-caused и закрыт через override.yml на сервере (PR #92 mirror in repo).** Root cause: `deploy/auto-deploy.sh:33` подмешивал `prod.yml` с hardcoded `CORE_AUTH=required` (побеждал `.env=disabled`). Override.yml побеждает prod.yml. Backlog #13 закрыт; #12 (test_core_client.py rewrite) остаётся. Дальше TASK C (Amazon timeout collision).
- 2026-05-27 (вечер): **TASK A inventory_parser fork закрыт.** PR #90 → master `8f1d5c4`. Worker iter1 (2 commits: pure `git mv` 47 файлов + rewrite imports + drop vendor) → Reviewer iter1 ACCEPT (без findings, все 6 worker conflict items cross-checked). Auto-deploy scope=services, все 4 контейнера healthy. **Prod smoke:** `POST /api/sourcing/search` с `desiredItem=blizzard` → 21 hit на 4 магазинах (rogers 5, 1shot 5, als 1, lancaster 4). `from app.inventory.core import ParserService` работает. **LOC delta:** 230 файлов, +1267 / -32 749 (net -31 482 в репо). 4 новых тест-файла = 31/31, полный `make services-test` 265+1. **Drive-by inline TL patch:** `.github/workflows/ci.yml` pin `pytest-asyncio<0.27` — CI поймал pre-existing flake в `test_core_client.py` (deprecated `asyncio.get_event_loop().run_until_complete()`), long-fix отдельным backlog-пунктом (`#12` в трекере). **TL judgment-calls:** Worker conflict items — `_sitka_catalog.py` minimal (10 vs 25 symbols) → follow-up TASK A2 как pre-req avito-аудита; 23 vs 20 stores reconciliation (моя ошибка acceptance) → синхронизировал TASK header; 4 compat shim'а перенесены без переписывания store-импортов (minimal scope); gitleaks-allowlist drive-by принят. Дальше TASK B (P0-1 substring drop в `BaseStoreAdapter.title_matches` через property-тесты).
- 2026-05-24 (вечер): инцидент — оператор не мог войти в кассу с правильным паролем. **Корень:** в `.env` `CORE_AUTH=disabled`, но в живом core-контейнере env-var оставалась `required` (с какого-то прошлого запуска). Docker не подхватывает `.env` изменения без recreate. Браузер → nginx (Basic Auth ОК) → core proxy_pass → core ждёт Bearer токен → 401 на всех `/api/*`. Главная страница `/` пускала, но любой API-запрос возвращал 401, оператор видел только prompt на пароль. **Лечение:** `docker compose --profile app up -d --no-deps --force-recreate core` — core подхватил `disabled`. Probe после fix: `/`, `/api/deals`, `/api/deals/stats`, `/api/settings/pricing` все 200 с правильным паролем; без пароля — 401. Downtime ~10 секунд. **Открытый вопрос для retro:** как и когда core оказался запущен с `CORE_AUTH=required`. Подозрение на `bash deploy/auto-deploy.sh` scope=services при PR #89 (видел в логах «Container sitka-office-core-1 Recreated», но env var не обновилась — значит `--force-recreate` не было, или env читается из старого compose-cache). Раскопать отдельно; пока в `OPERATING/backlog.md` → security/correctness.
- 2026-05-22 (ночь): закрыты обе задачи из почтового ящика 2026-05-20/21. **(б) PR #89 → master `ccac24b`** — `vendor/inventory_parser/inventory/v2/query.py:113` `<= 1` → `<= 2`, `tests/test_inventory_v2_query.py` (7 кейсов), drive-by `.github/workflows/ci.yml` editable install vendor-пакетов (CI падал ImportError при сборе теста; локально работало). Worker iter1 → Reviewer iter1 ACCEPT. Auto-deploy через `bash deploy/auto-deploy.sh` (scope=services), все контейнеры healthy. Smoke на проде: `blizzard parka` теперь возвращает 3 hits на rogers+1shot (главный товар «Blizzard AeroLite Parka» $538.30 со скриншота 2026-05-13 найден). Tier judgment-call B→C задокументирован в TASK header (vendored, одна строка, smoke = de-facto integration). **(в) Архитектурный аудит inventory_parser** — отчёт в `research/inventory-parser-architecture-audit-2026-05-21.md` (46 КБ, 7 секций), светофор ЖЁЛТЫЙ, 2 P0 + 4 P1 + 4 P2 + 4 NTH, главная рекомендация — **fork'нуть и владеть** (30K из 40K LOC vendor'а dead, 24 дня без re-vendor, P0 невозможно патчить чисто как vendor). Записка про аудит остаётся в живом ящике до решения Owner'а по форку. Доклад в `MAILBOX/to-admin.md`.
- 2026-05-20 (ночь Bishkek): общий аудит + блок production-safety фиксов того же дня. **P0 закрыт:** backup-сервис Sitka запущен впервые (раньше никогда не работал, база ни разу не дампилась), первый дамп `sitka_20260519_222020.sql.gz` (522 КБ), retention 7 дней. **P1 закрыты:** postgres bind на `127.0.0.1` (защита от повтора FounderOS-инцидента, временное правило `DOCKER-USER` снято); swap 2 ГБ добавлен; PR #88 зеркалирует server-only override.yml + nginx-server.conf в `deploy/`. Overlay refresh: `KNOWN_ISSUES.md` + `NEXT_ACTIONS.md` → snapshot `7b069a7`, DRAFT TASK `dm7-c-backend-widget-prereq` дописан `Mode: strict`. Astro: stale STOP-handoff перенесён в archive. Дата P0 c FounderOS в журнале поправлена: `2026-05-14` → `2026-05-17` (по git-timestamp).
- 2026-05-13: оператор натолкнулся на «парсер ничего не находит по `Blizzard parka`» (скриншот — 1shot). Диагностика: парсер исправен инфраструктурно, проблема в `vendor/inventory_parser/inventory/v2/query.py:113` — двусловные запросы попадают в `target` режим, который требует фразу substring в названии, а у валидных товаров между ключевыми словами стоит вставка («Blizzard AeroLite Parka» $538.30, «Sitka Blizzard Pro Parka» $800). Probe на сервере подтвердил: `blizzard` → 5 товаров на 1shot и Rogers (та самая парка из скриншота); `blizzard parka` → пусто на всех четырёх. По согласованию с оператором: workaround «вводи одно ключевое слово», fix отложен в backlog (Tier C ~1-3 LOC + тесты, секция «Backend / data integrity»). Первый поведенческий баг в парсере с момента vendoring 2026-04-27.
- 2026-05-14 (ночь): PR #87 автодеплой через GitHub Actions включён и протестирован → master `7b069a7`. **Политика деплоя owner'а 2026-05-14 (после явного запроса «зачем кнопка если агент сам может»):** в норме выкат делает **агент через SSH** (`ssh root@94.72.112.106 'cd /opt/sitka-office && bash deploy/auto-deploy.sh'`). Скрипт-подстраховка с scope detection + smoke + auto-rollback одинаково работает независимо от точки входа. Workflow `Deploy to production` (через Actions tab, manual `confirm: yes`) — **fallback** на случай когда агент недоступен, либо для Виталия (у него нет SSH-доступа на прод). Owner setup завершён: 5 secrets в env `production` + private key стёрт с сервера. Required reviewers недоступен на free tier personal-account — пропущен, остаётся первый рубеж (`confirm: yes` в input). Smoke прогон на пустом diff (docs-only scope) зелёный за ~7s. Manual fallback («Manual updates from main» в DEPLOY.md) тоже сохранён — для incident-расследований.
- 2026-05-13 (ночь): инцидент на проде → PR #86 (wget в Dockerfile core + точечные deploy-команды в DEPLOY.md) → master `42be920`. Триггер: деплой PR #83/#84 командой `docker compose up -d --build web` каскадно пересобрал `core` через `depends_on`, новый образ оказался без `wget`, healthcheck (`wget -qO- /health`) ушёл в `executable file not found`, `web`/`services` ждали healthy → ~7 минут downtime. Тёплый фикс — `apt-get install -y wget` в живой контейнер. Долгосрочный — этот PR (вернул `wget` в финальный stage + переписал «Updates from main» как три рецепта: frontend-only с `--no-deps`, backend через `--no-deps`, full deploy с каскадом). После merge `core` выкачен правильно (`build core && up -d --no-deps core`), все 4 контейнера healthy, `wget` теперь в самом образе. Параллельно в ai-dev-system — Correction 017 (`560c911`).
- 2026-05-13 (вечер): калькулятор парсера теперь учитывает буфер курса. PR #84 → master `f048c9b`. TASK `2026-05-13-parser-quote-calculator-exchange-buffer` (Tier B, Mode normal, Worker model Claude Code) — Worker iter1 (седьмое поле «Буфер курса, %» с дефолтом из `psExchangeBuffer`, эффективный курс через `usdRate × (1 + buffer/100)`, 21 unit-теста, два числовых эталона #1/#2 сошлись до копейки) → Reviewer iter1 ACCEPT (независимый пересчёт обоих эталонов в JS, scope strict 2 файла, 182/182 vitest + build чистый). Visual layout 7-польной сетки (3+3+1 на 638px sidebar) подтверждён через `npm run dev` в worktree. Два non-blocking findings Reviewer (F1 mock-helper dup, F2 placeholder format) — REJECT/DEFER, в backlog не плодим. Два residual'а из PR #83 (защита `usdRate<0`, cleanup `.parser-calculator-submit` + `offerSavedCount`) остаются в backlog как было. Backend (`Api.PricingSettings`, `Engine.Pricing`) не тронут.
- 2026-05-13: калькулятор парсера превращён из заглушки в рабочий инструмент, кнопка копирования итога добавлена, тесты зелёные. PR #83 → master `936ccdb`. TASK `2026-05-12-parser-quote-calculator` (Tier B, Mode normal, Worker model Claude Code) — Worker iter1 (6 inputs / 7 breakdown lines, 17 unit-тестов) → Reviewer iter1 ACCEPT (numeric correctness + scope clean + 178/178 vitest зелёный + `npm run build` clean). Четыре worker conflict'а (6 vs 7 полей; customs свёрнут в RU shipping default; exchange buffer не выведен отдельным input'ом; inline-style override `.parser-calculator-submit`) — приняты на TL-уровне. Три follow-up в backlog (см. ниже).
- 2026-05-11: deploy `sitka-perimeter-close` на сервер выполнен. `git pull` до `470f48f` (PR #81 в master), `/etc/sitka-htpasswd` создан (bcrypt-12, user `sitka-ops`), server-only `docker-compose.override.yml` обновлён (loopback bindings для core/services через `ports: !override`, volume mount htpasswd, локальный `deploy/nginx-server.conf` — HTTP-only адаптация `nginx-prod.conf` без SSL, поскольку домен/Let's Encrypt пока не настроены). `docker compose up -d --force-recreate web core services` прошёл, все 4 контейнера healthy. Внешний smoke с локального хоста: 8080/8081 → Connection refused; 8088/ без auth → 401; 8088/api/treasury/cashbox с auth → 200; 8088/services/* без auth → 401; 8088/health без auth → 200. Старый override-файл сохранён рядом как `.bak-may11` для отката.
- 2026-05-10 (вечер): TASK `sitka-perimeter-close` принята (Mode: strict, Risk tier A) — закрыт P1 perimeter gap **в репо**. Worker iter1 + Reviewer iter1 (REQUEST CHANGES — web healthcheck бил `/` за `auth_basic`) + Worker iter2 (override healthcheck на `/health` + Compose ≥ 2.20 pre-check в DEPLOY.md) + Reviewer iter2 (ACCEPT). 4 файла в product repo working tree; deploy сделан 2026-05-11 (см. выше).
- 2026-05-10: arbitration `codex-prod-audit-arbitration` принята и закрыта; HANDOFF и TASK перемещены в archive. Backlog по 4 принятым findings уже зафиксирован в `OPERATING/backlog.md` (коммит `b44ac5d`, 2026-05-09); P3 `expenseCategoryId` остаётся отложенным — триггер «оператор хочет breakdown» в существующем DRAFT `dm7-c-backend-widget-prereq.md`.
- 2026-05-09: memory/context cleanup — журнал событий перенесён в `OPERATING/journal/`, открытые мелочи — в `OPERATING/backlog.md`, dashboard сокращён до этого файла.
- 2026-05-02: Production live на 94.72.112.106:8088 (master `b58e5fb`); DM-7-C полностью закрыта. Подробности — `OPERATING/journal/2026-05.md` (раздел «Сводка EOD 2026-05-02»).
