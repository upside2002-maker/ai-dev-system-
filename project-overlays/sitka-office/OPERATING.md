# Sitka Operating

Живой dashboard оперативного состояния sitka-office. Обновляется TL или Admin **по событию** (новый TASK, новый HANDOFF, изменение Reviewer findings).

Последнее обновление: 2026-05-02 EOD (**Production live** на 94.72.112.106:8088, internal IP-only; PR #78 layout + #79 PGDG + #80 services-wget все merged, master HEAD `b58e5fb`).

## Сводка для старта следующей TL сессии (2026-05-01 EOD)

**Эта сводка — first read для новой сессии вместо reconstruct из переписки.**

**Master:** `5bc9bec` (Phase A/B/C Core + Frontend UI tab + DealFinancialBlock + cashbox live refresh + csTotalSpent backend + Касса top-tab + Variant B widget cells + slug auto-gen с RU translit — **DM-7-C полностью закрыта**).

**Открытых PR нет.** За 2026-05-01 закрыто 4 PR подряд:
- PR [#74](https://github.com/upside2002-maker/sitka-office/pull/74) `feat/dm7-c-frontend-ui-tab` → master `25b7fb4` (06:13Z, +1646 LOC).
- PR [#75](https://github.com/upside2002-maker/sitka-office/pull/75) `fix/dm7-c-deal-financials-and-cashbox-refresh` → master `788f6b0` (06:24Z, rebased через `gh pr update-branch --rebase` после #74).
- PR [#76](https://github.com/upside2002-maker/sitka-office/pull/76) `feat/dm7-c-cstotalspent-backend` → master `43ebf14` (14:50Z, +46 LOC, 539/539 tests).
- PR [#77](https://github.com/upside2002-maker/sitka-office/pull/77) `feat/dm7-c-cashbox-tab-and-variant-b` → master `5bc9bec` (21:44Z, +280/-101 LOC, 166/166 vitest + 41/41 Playwright).

**Следующая работа (только опциональная):**
1. **`trExpenseCategoryId` backend prereq** ([`TASKS/2026-04-29-dm7-c-backend-widget-prereq.md`](TASKS/2026-04-29-dm7-c-backend-widget-prereq.md)) — `Status: open` → **DRAFT** → Worker. Pre-req для **далёкого** Phase D widget breakdown по `expense_category.kind` (не текущий план). Маленький TASK ~30 LOC backend Tier A. Запускать только когда оператор реально захочет breakdown в виджете — backend поле live unused до этого момента.

**Phase D (за горизонтом):** widget breakdown по `kind` (после backend prereq merged), графики/аналитика по cashbox в Касса tab, transactions ledger viewer с фильтрами. Без явного operator UX триггера — не приоритет.

**Принятые продуктовые решения (не пере-обсуждать без явного триггера):**
- Threshold «+ Расход на доставку» = `status ∈ {InKazakhstan, ShippedRu, Delivered}` — в master через PR #75.
- Cashbox = первичная сущность системы, видна на двух уровнях: summary в `AnalyticsDashboard` (Сделки tab) + полноценный модуль в новом «Касса» tab. Position tab = 2-я в nav (после Сделки, до Сообщения).
- Variant B cashbox cells = `Всего / Свободно / В резерве / Потрачено / Прибыль закрытых сделок`. Running-cashbox-margin **не** вводим. `csExpectedMargin` остаётся в backend и TS type — просто не рендерится в widget.
- `csExpectedMargin` formula `quoted − plannedCost − reservation.planned` (plan-based, PR #70) — корректна.
- Slug в `ExpenseCategoryManager` — авто-генерация из displayName с RU→Latin transliteration (inline ~30 LOC, GOST-style simplified). Slug input + slug column в table — оба скрыты от оператора. Cell variant для «Потрачено» = новый `'spent'` (не reuse `'negative'`).
- Slug-conflict 409 even on archived (no auto-reactivate); `tx_date` cap NOW + 60s skew.

**Защищённые invariants в master (не relaxing):**
- Free-balance guard на `createManualExpense` (PR #72 + #75 follow-up).
- Generic `POST /api/treasury/transactions` rejects `OperationalExpense + SourceManual` (PR #72 + #75).
- Plan-based `csExpectedMargin` (не возвращать к running-diff Phase A формуле).
- Tri-state Aeson PATCH для `umeArchivedAt` (PR #74).
- `<DealFinancialBlock>` unified в Confirmed step + cashbox live refresh через `useEventStream` (PR #75).

**Open backlog (mini-TASKs, не блокеры):**
- **Mobile responsive** (отложено 2026-05-02 per user) — frontend desktop-first, 23 `@media` rules покрывают workspace-shell/funnel но Касса tab + Variant B widget + ExpenseCategoryManager без mobile-стилей. Tier C ~100-300 LOC + audit всех 6 tabs на 375px viewport.
- **Mirror server inline patches в `docker-compose.prod.yml`** — на сервере `/opt/sitka-office/docker-compose.override.yml` имеет: `web ports !override 8088:80`, `LANG/LC_ALL=C.UTF-8` в core+services (чтобы dev-format Russian logs не крашили `commitBuffer: invalid argument`), `CORE_DB_CONN` в services env (knowledge_search нужен прямой DB access), `CORE_CORS_ORIGINS` с prod URL. Без mirror в repo — каждый next deploy будет повторять inline patches вручную. Tier C ~30 LOC.
- **web container wget healthcheck** — busybox wget работает, но docker reports unhealthy после ранних failed checks (cosmetic; функция OK). Mini-PR ~5 LOC чтобы поменять healthcheck на `wget --spider` или TCP probe.
- Messages layout bug — header перекрывает top-nav. Ждёт скрин для точной диагностики. Tier C, ~20 LOC.
- `recordEvent` на `manual_expense` insert (Tier A backend, ~10-15 LOC) — чтобы CashboxWidget refreshed после manual expense (currently не event-driven).
- `_dealId` unused в `client.ts:304` — pre-existing master lint error (hygiene).
- `useEventStream` singleton refactor — если subscribers вырастут до 4+ (Phase D widgets); после T3+T4 = 2 subscribers (CashboxWidget mounted в AnalyticsDashboard + CashboxScreen).
- CSS classes `.ws-financial-block` / `.ws-kv-line--muted` определения (косметика, fallback работает).
- **Frontend bearer auth flow** — сейчас prod использует `CORE_AUTH=disabled` + `SITKA_API_TOKEN=""` потому что frontend никогда не реализован для bearer header. Если когда-нибудь захочется external exposure (DNS+SSL+Let's Encrypt phase 5) — нужен frontend auth flow ~50-100 LOC + token storage. На internal IP-only сейчас не критично.

**Backend prereq DRAFT (отдельный TASK файл, НЕ для Variant B):**
- `TASKS/2026-04-29-dm7-c-backend-widget-prereq.md` — `trExpenseCategoryId :: Maybe Int64` в `TransactionResp` wire shape. Для **далёкого** Phase D widget breakdown по `kind` (не текущий план).

**Локальное окружение:** PG @5432 (Docker), sitka-core @8080 (PID 31891 на момент 2026-04-30 EOD — проверь `lsof -iTCP:8080`), vite @5173 (PID 52130 на момент закрытия EOD — ветка была `fix/dm7-c-deal-financials-and-cashbox-refresh`, теперь её remote удалена, локальный worktree `upbeat-maxwell-463203` ещё держит ветку — почистить когда удобно). При запуске Worker'а на csTotalSpent backend — vite не нужен; только PG + опционально sitka-core для smoke.

**Worktree cleanup:** `upbeat-maxwell-463203` держит локальный branch `fix/dm7-c-deal-financials-and-cashbox-refresh` (remote deleted после merge); снять worktree когда удобно (`git worktree remove`). Не блокер.

**Active TASKS / Reviewer findings / HANDOFFS — см. ниже в этом файле.**

---

## Текущая фаза

DM-7 cashbox refactor. **Phase A, Phase B и Phase C закрыты целиком** (PR #63–#66 для A/B + live-smoke #68–#70 + Phase C Core #72/#73 + Phase C Frontend #74/#75/#77/#78 + backend prereq #76). Production live на `94.72.112.106:8088` после deploy fixes #79/#80. Phase D — scope в `/Users/ilya/Projects/sitka-office/docs/DM-7-cashbox.md`, в overlay не зафиксировано.

Подробности — в [`CURRENT_STATE.md`](CURRENT_STATE.md). Сводка для старта следующей TL-сессии — в секции "Сводка" выше.

## Активные TASKS

- [`TASKS/2026-04-29-dm7-c-backend-widget-prereq.md`](TASKS/2026-04-29-dm7-c-backend-widget-prereq.md) — `Status: open` → **DRAFT** → Worker → расширить `Api.TransactionResp` полем `trExpenseCategoryId :: Maybe Int64`. Pre-req для future Phase D Widget breakdown TASK по `expense_category.kind`. **НЕ срочно** — запускать только когда оператор захочет breakdown в виджете.

См. [`TASKS/README.md`](TASKS/README.md) для правил.

## Открытые HANDOFFS

(нет)

См. [`HANDOFFS/README.md`](HANDOFFS/README.md) для правил.

## Reviewer findings ожидающие TL

(нет)

## Активные Worker сессии

(нет в этом моменте)

## Заметки

- 2026-05-02 EOD: **Production smoke + iteration после initial deploy.** (1) **Phase 4a knowledge seed:** `make seed-knowledge` после `apt-get install -y python3-psycopg` на host → 2553 sections / 284 моделей в `knowledge_chunk`. `/services/api/knowledge/search?query=jacket` → 435 hits ✓. (2) **Phase 4b Avito channel + polling enable:** `POST /api/marketing/channel-accounts {channel:avito, accountName:avito-prod}` → carId=1 → set `SITKA_AVITO_CHANNEL_ACCOUNT_ID=1` + flip `POLLING_ENABLED=true` + `SENDER_ENABLED=true` + перенёс `SITKA_APIFY_TOKENS` из local → restart services → poller/sender/bot/parser все running. (3) **Avito backfill:** `PUT /api/service-state/avito:last_seen_at {usrValue:"2026-02-01T00:00:00Z"}` → reset cursor → next poll-tick подтянул **1102 historical messages** за один tick (`Avito poller: ingested 1102 new messages`). Cursor дотягивает оставшиеся в последующих циклах. (4) **CORS fix:** wai-cors отвергал same-origin POST с `Unsupported origin: http://94.72.112.106:8088` (browser шлёт Origin header для всех POST/PUT/DELETE даже на same-origin, wai-cors validate'ит против whitelist). Default `CORE_CORS_ORIGINS=http://localhost:5173,http://localhost`, prod URL не в нём → reject. Fix в `docker-compose.override.yml` на server: добавил `CORE_CORS_ORIGINS=http://localhost:5173,http://localhost,http://94.72.112.106:8088` в core env. **TODO в master:** mirror в `docker-compose.prod.yml` чтобы next deploys автоматом включали prod URL (либо принимать через env). (5) **Mobile responsive — отложено per user request.** Frontend desktop-first, 23 `@media` rules в 3217 LOC CSS coverage fragmentary (Касса tab + Variant B widget + ExpenseCategoryManager без mobile-стилей). Полноценная адаптация = отдельный TASK Tier C ~100-300 LOC когда вернётся приоритет. (6) **PR #80 merged** (master `b58e5fb`, 01:17Z UTC).
- 2026-05-02 ~16:00: **First production deploy на VPS 94.72.112.106 (Hetzner-style, Ubuntu 24.04, root, docker 29.4.2 + compose v5.1.3 + dbmate 2.33).** Internal-only IP-based (`http://94.72.112.106:8088`), без DNS/SSL/Let's Encrypt — system nginx занят founderos:443 + sitka-qa:80 (НЕ трогаем). Решение Option C из обсуждения: docker compose web на 8088 через `docker-compose.override.yml` (server-only, не git-tracked) с `!override` directive для замены `ports` instead of merge (compose v2.20+ syntax). По дороге найдены и пофикшены 6 production-only багов:
  - **PR [#78](https://github.com/upside2002-maker/sitka-office/pull/78) cashbox layout** (merged 13:21Z, master `143a1de`) — `.workspace-shell--cashbox` CSS missing, виджет был squeezed в 280px inbox column; добавлен `grid-template-columns: 1fr` mirror других views. Plus drop dup h2.
  - **PR [#79](https://github.com/upside2002-maker/sitka-office/pull/79) Dockerfile PGDG repo** (merged 15:24Z, master `3ab30d1`) — `haskell:9.6-slim` = bullseye = libpq 13, `persistent-postgresql` 2.14.3 хочет ≥14.12. Switch к PGDG repo для libpq 16+. CI mask'ил (uses ubuntu-latest host apt, не Docker builder).
  - **PR [#80](https://github.com/upside2002-maker/sitka-office/pull/80) services Dockerfile wget** (open, awaits CI) — `python:3.12-slim` без wget, healthcheck падал с exit 127 → `web` blocked by `depends_on: services healthy`. Same pattern, mirror inline patched на server.
  - **Inline на server (НЕ в master, server-only):** `LANG=C.UTF-8`/`LC_ALL=C.UTF-8` в core+services через override (без этого core крашится `commitBuffer: invalid argument` на em dash в dev-format Russian logs); `CORE_DB_CONN` в services env через override (knowledge_search нужен прямой DB access); web container's busybox wget работает но healthcheck cosmetic stuck unhealthy после ранних failed checks (функция OK, не fix'ил).
  - **Productivity decisions:** `CORE_AUTH=disabled` + `SITKA_API_TOKEN=""` (frontend никогда не реализован для bearer; internal tool на private VPS, security risk минимален; bearer flow = отдельный TASK если когда-нибудь нужен external exposure). `CORE_CORS_ORIGINS` дефолтный (same-origin SPA на 8088 не нужен CORS). Local dev НЕ polling Avito/Telegram (sitka-services контейнер не запущен у user'а на Mac), race с prod нет — единственный poller на проде.
  - **Phase 4b (channel account) выполнен:** POST /api/marketing/channel-accounts → carId=1 (avito-prod), `SITKA_AVITO_POLLING_ENABLED=true` + `SITKA_AVITO_SENDER_ENABLED=true` + `SITKA_APIFY_TOKENS` (из local) → restart services → poller/sender/bot/parser все running.
  - **Phase 4a knowledge seed выполнен:** `make seed-knowledge` (после `apt-get install python3-psycopg system-wide`) → 2553 sections / 284 моделей. `/services/api/knowledge/search?query=jacket` → 435 hits.
  - **Final stack 16:00Z:** postgres + core + services healthy; web up (cosmetic unhealthy на wget). External `http://94.72.112.106:8088` returns 200 OK SPA. All 4 tabs работают (Сделки + Касса + Сообщения + Парсер + БЗ + Маркетинг). Avito poller + Telegram bot live.
- 2026-05-02 ~13:21: **PR #78 cashbox layout fix merged** (master `143a1de`). TL inline patch (≤30 LOC, Tier C UI) — обнаружено в manual smoke сразу после PR #77: AppLayout добавлял `workspace-shell--cashbox` className но CSS rule отсутствовал → CashboxScreen squeezed в 280px inbox column. Fix: `grid-template-columns: 1fr` mirror `--settings/--knowledge/--parser`. Plus dropped duplicate `<h2>Касса</h2>` из CashboxScreen header (виджет card title уже подписан). Diff 24 LOC, 2 файла. CI 8/8 green.
- 2026-05-01 ~21:45: **PR #77 T3+T4 IA refactor + Variant B + slug translit merged** (`5bc9bec`). **DM-7-C полностью закрыта.** Worker сдал HANDOFF v1, acceptance A-J ✓ (12 файлов, +280/-101 ≈ 381 LOC под 400-cap, vitest 166/166 + Playwright 41/41 + tsc clean + lint clean baseline = master). Diff verify: Variant B cells в правильном порядке (Всего→Свободно→В резерве→Потрачено→Прибыль закрытых), CSS `cashbox-cell--spent` = muted amber `#c47a1d` (background rgba(196,122,29,0.07), value color #c47a1d) — distinguishable от red/green/yellow ✓; AnalyticsDashboard НЕ в diff (anti-decision соблюдён); TRANSLIT_MAP RU GOST simplified inline ~6 строк (без npm dep); slug input + table column оба скрыты; tab «Касса» = 2-я кнопка (между Сделки и Сообщения), data-testid `view-switcher-cashbox`. CashboxScreen 54 LOC mounts `<CashboxWidget />` сверху + `<OperationalExpenseManager />` снизу с auto-dismiss banner 3500ms. **Worker flagged in-scope adjustment loud (Rule #1 ✓):** `useAppState.ts:44-51` +1 строка (`'cashbox'` в `useState<>` union, type-only consequence от §A1 расширения WorkspaceContext.view union; без него tsc fail). Accepted inline — refactor через context был бы дороже. **Worker miss (Rule #5 lesson 2-й раз):** в HANDOFF Worker написал «7/7 blocking lanes pass + Coverage non-blocking pending» — фактически 8/8 включая Coverage (20m31s success). Не блокер; small lesson — `gh pr checks` перед HANDOFF, не assumption. **6 «замечено по дороге» — все 4 in scope:** TRANSLIT_MAP RU only / cashbox-screen CSS fallback на mk-root / recordEvent на manual_expense gap / pre-existing master lint errors — все либо явно в TASK §«Условные возвраты», либо уже в OPERATING backlog. Lifecycle: TASK + HANDOFF → archive с Status: done. CURRENT_STATE.md snapshot обновлён `43ebf14 → 5bc9bec`. **Активные TASKS = 1** (далёкая backend prereq, не срочно).
- 2026-05-01 ~14:55: **PR #76 csTotalSpent backend merged** (`43ebf14`). Worker сдал HANDOFF v1, acceptance A-D ✓ (3 файла, +46 LOC под 80-cap, 539/539 tests). Diff verify: поле `csTotalSpent :: RUB` в правильной позиции (после `csExpectedMargin`), builder reuse существующих helpers (`negateRub / sumAmounts / amountIs`, без новых), Haddock 5 строк, 9 fixture updates с inline комментами объясняющими expected (math spot-check 55000+7300=62300 ✓), E6 integration test pin'ит 4 contract'а (baseline=0 после OwnerDeposit, 3 expenses → 15000, JSON Double, sign convention round-trip backend-negate→engine-renegate). Property test untouched (использует single-field `===`). Admin merge через `--admin --squash` work'нул при Coverage=pending (non-blocking lane). Local branch hold-up на worktree `upbeat-maxwell-463203` — не блокер. **Worker miss (Rule #5 grep verify):** в HANDOFF Worker написал «CI red от billing block» — фактически 7/8 lanes уже passed когда я проверил, билинг сброшен ещё утром. Worker не grep'нул PR checks перед HANDOFF; small lesson, не блокер. Lifecycle: TASK + HANDOFF → archive с Status: done. CURRENT_STATE.md snapshot обновлён `788f6b0 → 43ebf14`. **T3+T4 IA refactor разблокирован — ready для Worker handoff.**
- 2026-05-01 ~06:25: **Билинг сброшен → оба заблокированных PR закрыты.** Sequence: (1) `gh run rerun 25178912549` + `25181191988` — оба jobs реально стартовали (steps non-empty, не billing-stub). (2) Watch background ~10 мин, оба run'а completed success (8/8 lanes). (3) `gh pr merge 74 --squash --delete-branch --admin` → master `25b7fb4`. (4) `gh pr merge 75 --squash --delete-branch --admin` → REJECTED («7 of 7 required status checks expected» — после #74 merge статусы #75 stale; admin не bypass'нул потому что checks expected = pending, не failed). (5) `gh pr update-branch 75 --rebase` → новый SHA `9064a74`, новый CI run `25204745583`. (6) Watch ~10 мин, run completed success (8/8). (7) `gh pr merge 75 --squash --delete-branch --admin` → master `788f6b0`. Локальный branch `fix/dm7-c-deal-financials-and-cashbox-refresh` не удалился (worktree `upbeat-maxwell-463203` его держит) — не блокер. TASK + HANDOFF файлы обоих PR → archive с Status: done. CURRENT_STATE.md snapshot обновлён `51fd8ef → 788f6b0`. **Lesson:** PR #75 BEHIND после merge #74 = stale required checks → admin-merge fails. Решение паттерн: после первого merge, для каждого следующего PR обязательно `update-branch --rebase` + жди green CI. Учесть в будущих multi-PR sequences.
- 2026-04-30 ~17:30: TL session start — drift check master `51fd8ef` ✓ (matches snapshot). Билинг-блок CI **подтверждён** на обоих PR (#74 `abee049` 2026-04-30 17:10 + #75): annotation `"job was not started because recent account payments have failed"`, all 8 lanes failed в 2-10 сек, `steps[]` empty (jobs не стартовали). Локальный env жив (sitka-core PID 31891 @8080, vite PID 52130 @5173). Курс: ждём reset, в parallel созданы два DRAFT TASK.
- 2026-04-30 ~17:45: **IA decision уточнён пользователем после первого draft TASK.** Cashbox = первичная сущность системы, видна на двух уровнях одновременно: (1) `<CashboxWidget />` summary остаётся в `AnalyticsDashboard.tsx:45` (Сделки tab, persistent на главной); (2) новый top-tab «Касса» с `CashboxScreen` (тот же widget сверху + OperationalExpenseManager снизу = детализация). **Position tab** в top-nav — между «Сделки» и «Сообщения» (Касса = 2-я, financial visibility рядом с workspace pulse, НЕ в конце после Маркетинга). «Графики и аналитика» — explicit out-of-scope T3+T4, backlog Phase D. TASK B (`cashbox-tab-and-variant-b.md`) обновлён под двух-уровневую раскладку: AnalyticsDashboard НЕ трогается (мой первый draft ошибочно требовал «delete» — переписан); CashboxScreen mounts тот же CashboxWidget + OperationalExpenseManager. `useEventStream` будет иметь 2 subscribers (within tolerance до 4+).
- 2026-04-30 ~18:00: **Финальные micro-decisions подтверждены пользователем (3 override + 6 accept):** (#5) slug **column в table** тоже скрыта (не только form input); (#7) для cell «Потрачено» **обязательный отдельный variant `'spent'`** (не reuse `'negative'`, не «на усмотрение Worker'а»); CashboxCell union обновлён `'primary' | 'reserved' | 'positive' | 'negative' | 'spent'` — `'expected'` дроп; (#8) **cyrillic→latin transliteration включена** (inline TRANSLIT_MAP ~30 LOC, GOST-style simplified, без npm dep), `Зарплата март Q1` → `zarplata_mart_q1` etc. Accepted as-is: #1 position поля в record (после csExpectedMargin); #2 TS update в FE TASK не backend; #3 один E6 integration test; #4 Касса 2-я в nav (после Сделки); #6 csExpectedMargin держим в TS type; #9 e2e на усмотрение Worker'а. **TASK B готов к Worker handoff после billing reset + merge #74 + #75 + csTotalSpent backend.**
- 2026-04-29 02:30: **DM-7-C Core закрыта**. PR [#72](https://github.com/upside2002-maker/sitka-office/pull/72) merged squash + admin (master `4ce5694` → `6c91ef3`). 2 коммита (TASK-1 `03ae50d` foundation + TASK-2 `3c30caf` post-Reviewer fixes), 5 файлов, 920/67. CI 8/8 SUCCESS, 538/538 tests. Закрыто: 4 endpoint-а Treasury (categories CRUD + manual expense POST), tri-state PATCH Aeson (новый паттерн в репо), free-balance guard в `createManualExpense`, reject `OpEx+Manual` в generic /transactions, refactor `guardFreeBalanceAfter*` из `Api.Deals` → `Api.Treasury` (4 call sites обновлены). Codex Reviewer round (2 P1 findings) закрыт через TASK-2. TASK-1, TASK-2, HANDOFFS v1/v2/v3, Reviewer report → archive.
- 2026-04-29 02:39: TL doc-sync **закрыт**. PR [#73](https://github.com/upside2002-maker/sitka-office/pull/73) merged squash + admin (master `6c91ef3` → `51fd8ef`). `docs/DM-7-cashbox.md` синхронизирован (header Phase A/B/C Core, таблица фаз C-Core ✅ / C-Frontend TODO, Resolved invariants пункт 8). 1 файл, 11 строк. `CURRENT_STATE.md` overlay обновлён.
- 2026-04-29 ~14:00: **PR #74 (Phase C Frontend UI) CI заблокирован GitHub Actions billing.** Все 7 lanes аннотированы «recent account payments have failed or your spending limit needs to be increased». Это НЕ Worker bug — код локально полностью зелёный (vitest 150/150, Playwright 41/41, tsc -b clean, pre-commit clean). Билинг сбрасывается ~24h. **Курс — α (wait):** не force admin merge, не обходить CI gate ради одного дня; вместо этого использовать время для подготовки следующих TASK. После reset: re-trigger CI на PR #74 (push любого commit или manual re-run) → admin squash merge на green → параллельно запуск Worker'а на backend prereq TASK. **Backend Prereq TASK draft создан** (`TASKS/2026-04-29-dm7-c-backend-widget-prereq.md`, Status: open, awaits Worker start после reset). Frontend Widget TASK будет создан после accept обоих (PR #74 + backend prereq).
- **Backlog: Frontend Widget TASK (последняя связка Phase C, Tier C ~80 LOC).** Расширить [sitka-web/src/components/CashboxWidget.tsx](sitka-web/src/components/CashboxWidget.tsx) с разбивкой outgoing по `expense_category.kind` (4 группы: salary/ops/logistics/misc) через клиентский join `transactions × categories` — теперь возможно после backend prereq добавит `trExpenseCategoryId` в wire shape. Создаётся как новый TASK после merge PR #74 + backend prereq.
- **Backlog (drive-by для будущего T3+T4 TASK — IA refactor «Касса» tab):** при переезде `CashboxWidget` в новый top-tab «Касса» обязательно включить (TL pивший 2026-04-30 после второго раунда operator UX feedback + Codex argument через grep verification, **Variant B** выбран):
  - **Cashbox cells layout — Variant B**: 5 ячеек = `Всего` / `Свободно` / `В резерве` / **`Потрачено`** (новая) / `Прибыль закрытых сделок` (`csRealizedProfit`). Drop `Ожидаемая маржа` (`csExpectedMargin`) из widget. Rename "Ожидаемая → Плановая" cancelled (теперь просто drop). Per-deal живая маржа остаётся в `<DealFinancialBlock>` (PR #75) — на cashbox-уровне running-margin **не** вводим.
  - **Backend rationale:** `csExpectedMargin` остаётся в `CashboxSnapshot` (backend), просто не отрисовывается в widget. Поле может пригодиться для Phase D финансовых отчётов / отдельного planning widget. Drop из widget ≠ drop из API contract.
  - **Обновить устаревший комментарий** [`CashboxWidget.tsx:16`](sitka-web/src/components/CashboxWidget.tsx) — сейчас описывает Phase A формулу «partial P&L for active deals minus their unused reservation slack», что устарело с PR #70. После T3+T4 widget вообще не показывает Expected Margin — комментарий перепишется под актуальные 5 cells.
  - **Опционально:** обновить `data.csExpectedMargin` JSDoc/inline-doc в [api/types.ts](sitka-web/src/api/types.ts) если там есть устаревшее описание (Worker grep'нет).
- **Backend mini-TASK PRE-REQ для T3+T4 «Потрачено»:** добавить `csTotalSpent :: RUB` в `Engine.Treasury.CashboxSnapshot` + wire shape `Api.TransactionResp` (если нужно — скорее всего расширение CashboxSnapshot wire через `Api.Types.hs`). Формула: `negate (SUM (transaction.amount WHERE amount < 0))` per-period (вероятно весь ledger или scoped к active deals — TL уточнит при создании TASK). Tier A money-flow, ~15-25 LOC + property test + integration test. Без миграций (схема готова). Создаётся вместе с T3+T4 (paralle TASK либо blocking pre-req — TL решит при оформлении).
- **Backlog (UI bug, отдельный mini-TASK Tier C frontend):** Messages screen — header / details лида перекрывают / залезают под top-nav, текст плохо читается. Скрин был упомянут в operator feedback 2026-04-30 но не дошёл до TL session (физический attachment не передан). Кандидаты root-cause до скрина: top-nav `position: sticky/fixed` с `z-index` выше Lead detail header; либо `padding-top` body не учитывает высоту top-nav. Файлы: вероятно `sitka-web/src/components/message-inbox/MessageInbox.tsx` + общий CSS top-nav. Tier C, ~20 LOC CSS / layout fix. Готов перевести в TASK когда придёт скрин или уточнение какие элементы перекрываются.
- **Backlog (drive-by из PR #75 HANDOFF v1, accepted as ОТЛОЖИТЬ):**
  - **CSS-PR (Tier C, ~10 LOC):** определить `.ws-financial-block` + `.ws-kv-line--muted` в `sitka-web/src/styles/*.css` (или эквиваленте) для visual muted look reservation-progress строки в `<DealFinancialBlock>`. Текущий fallback к base `.ws-summary-block` / `.ws-kv-line` функционально работает.
  - **`recordEvent` на `manual_expense` insert (Tier A backend, ~10-15 LOC + 1 test):** добавить вызов в `Api.Treasury.createManualExpense` (PR #72) чтобы CashboxWidget live refresh от PR #75 срабатывал и на manual expenses. Сейчас manual expense не пишет event, оператор видит обновление при следующем mount. Acceptable на solo, но для полной live reaction — нужен backend mini-TASK. Сложность: `manual_expense` без `dealId` — нужно либо ALLOW NULL FK на event, либо использовать sentinel deal или новый event scope. Требует grounding read `Domain.Lead.Event` / `recordEvent` shape прежде чем оформить TASK.
  - **`useEventStream` singleton refactor (Tier C, ~30 LOC):** сейчас 2 independent timers (useAppState + CashboxWidget) — на 30 deals/month негligible. Если subscribers вырастут до 4+ (Phase D widgets) — выделить в shared singleton.
  - **`_dealId` unused в `client.ts:304`** — pre-existing master lint error, не связано с DM-7-C. Hygiene fix, отдельный mini-TASK или drive-by в любом будущем PR трогающем `client.ts`.
- Backlog (kind: hygiene): doc-комменты в `Api/Deals.hs:~1244,~1247` ссылки на guards после refactor (`Treasury.guard*` префиксы — Haddock не валидируется CI, semantically стоит почистить); `Makefile` target `db-test-reset` для локальной DB ownership (`sitka_test owned by ilya vs sitka` ломает `setupPool`).
- 2026-04-28 16:30: TASK `2026-04-28-dm7-c-treasury-manual-expenses` — Worker вернул HANDOFF v1 на test-infrastructure блокере (TASK §Файлы указывал `TreasurySpec.hs` для integration tests, но harness живёт в `ApiSpec.hs`). TL принял **Option 1**: §Файлы расширены добавлением `ApiSpec.hs` (E1-E5 туда), `TreasurySpec.hs` остаётся (E6 property туда). Разрешён helper `requireExpenseCategory`. Worker может продолжать с той же сессии после re-read обновлённого TASK. HANDOFF v1 → archive после accept HANDOFF v2.
- Backlog (hygiene, kind: AI Dev System Admin): `scripts/file-tier.sh sitka-core/src/Api/Treasury.hs` возвращает `C`, хотя `.claude/risk-tiers.md` явно перечисляет файл в Tier A. Баг matcher-а в скрипте. Кандидат на отдельный TASK для maintainer (Tier C, ~10 строк bash). Замечено Worker'ом 2026-04-28.
- 2026-04-28: smoke-цикл нового процесса (TL → TASK → Worker → HANDOFF → TL accept) закрыт. TASK `2026-04-27-dm7-cashbox-doc-sync` accept, PR [#71](https://github.com/upside2002-maker/sitka-office/pull/71) merged (squash + admin-bypass — solo-репо: PR review недостижим из-за self-approve restriction, master ruleset обходится admin → ожидаемое поведение). 5/5 acceptance, diff 10 строк (≤15), CI 7/7 SUCCESS. Worker возвратов в TL не делал; 3 наблюдения «Замечено по дороге» отложены до следующей правки `docs/DM-7-cashbox.md` по существу (Phase C/D).
- Прошлый snapshot был `089191d`, текущий `39873d2` — за интервал 6 PR-ов (Phase B-2/B-3 завершение через #65/#66, vendor inventory parser #67, live-smoke fixes #68–#70). Подробности — в `CURRENT_STATE.md`.
- Старые `START_SITKA_ANALYST.md` и `START_SITKA_CODER.md` ещё работают (миграция в Фазе 3 ROLE_MODEL).
- Новые точки входа: `START_SITKA_TECH_LEAD.md`, `START_SITKA_BUSINESS_ANALYST.md`, `START_SITKA_REVIEWER.md`, `START_SITKA_WORKER.md`.
