# Sitka Operating — Журнал событий 2026-05

История событий по datestamp. Не редактируется задним числом — только дополняется новыми записями (если приходят с опозданием) с явной пометкой даты добавления. Текущий dashboard — `../../OPERATING.md`. Backlog (недатированные мелочи) — `../backlog.md`.

---

- 2026-05-01 ~06:25: **Билинг сброшен → оба заблокированных PR закрыты.** Sequence: (1) `gh run rerun 25178912549` + `25181191988` — оба jobs реально стартовали (steps non-empty, не billing-stub). (2) Watch background ~10 мин, оба run'а completed success (8/8 lanes). (3) `gh pr merge 74 --squash --delete-branch --admin` → master `25b7fb4`. (4) `gh pr merge 75 --squash --delete-branch --admin` → REJECTED («7 of 7 required status checks expected» — после #74 merge статусы #75 stale; admin не bypass'нул потому что checks expected = pending, не failed). (5) `gh pr update-branch 75 --rebase` → новый SHA `9064a74`, новый CI run `25204745583`. (6) Watch ~10 мин, run completed success (8/8). (7) `gh pr merge 75 --squash --delete-branch --admin` → master `788f6b0`. Локальный branch `fix/dm7-c-deal-financials-and-cashbox-refresh` не удалился (worktree `upbeat-maxwell-463203` его держит) — не блокер. TASK + HANDOFF файлы обоих PR → archive с Status: done. CURRENT_STATE.md snapshot обновлён `51fd8ef → 788f6b0`. **Lesson:** PR #75 BEHIND после merge #74 = stale required checks → admin-merge fails. Решение паттерн: после первого merge, для каждого следующего PR обязательно `update-branch --rebase` + жди green CI. Учесть в будущих multi-PR sequences.

- 2026-05-01 ~14:55: **PR #76 csTotalSpent backend merged** (`43ebf14`). Worker сдал HANDOFF v1, acceptance A-D ✓ (3 файла, +46 LOC под 80-cap, 539/539 tests). Diff verify: поле `csTotalSpent :: RUB` в правильной позиции (после `csExpectedMargin`), builder reuse существующих helpers (`negateRub / sumAmounts / amountIs`, без новых), Haddock 5 строк, 9 fixture updates с inline комментами объясняющими expected (math spot-check 55000+7300=62300 ✓), E6 integration test пин'ит 4 contract'а (baseline=0 после OwnerDeposit, 3 expenses → 15000, JSON Double, sign convention round-trip backend-negate→engine-renegate). Property test untouched (использует single-field `===`). Admin merge через `--admin --squash` work'нул при Coverage=pending (non-blocking lane). Local branch hold-up на worktree `upbeat-maxwell-463203` — не блокер. **Worker miss (Rule #5 grep verify):** в HANDOFF Worker написал «CI red от billing block» — фактически 7/8 lanes уже passed когда я проверил, билинг сброшен ещё утром. Worker не grep'нул PR checks перед HANDOFF; small lesson, не блокер. Lifecycle: TASK + HANDOFF → archive с Status: done. CURRENT_STATE.md snapshot обновлён `788f6b0 → 43ebf14`. **T3+T4 IA refactor разблокирован — ready для Worker handoff.**

- 2026-05-01 ~21:45: **PR #77 T3+T4 IA refactor + Variant B + slug translit merged** (`5bc9bec`). **DM-7-C полностью закрыта.** Worker сдал HANDOFF v1, acceptance A-J ✓ (12 файлов, +280/-101 ≈ 381 LOC под 400-cap, vitest 166/166 + Playwright 41/41 + tsc clean + lint clean baseline = master). Diff verify: Variant B cells в правильном порядке (Всего→Свободно→В резерве→Потрачено→Прибыль закрытых), CSS `cashbox-cell--spent` = muted amber `#c47a1d` (background rgba(196,122,29,0.07), value color #c47a1d) — distinguishable от red/green/yellow ✓; AnalyticsDashboard НЕ в diff (anti-decision соблюдён); TRANSLIT_MAP RU GOST simplified inline ~6 строк (без npm dep); slug input + table column оба скрыты; tab «Касса» = 2-я кнопка (между Сделки и Сообщения), data-testid `view-switcher-cashbox`. CashboxScreen 54 LOC mounts `<CashboxWidget />` сверху + `<OperationalExpenseManager />` снизу с auto-dismiss banner 3500ms. **Worker flagged in-scope adjustment loud (Rule #1 ✓):** `useAppState.ts:44-51` +1 строка (`'cashbox'` в `useState<>` union, type-only consequence от §A1 расширения WorkspaceContext.view union; без него tsc fail). Accepted inline — refactor через context был бы дороже. **Worker miss (Rule #5 lesson 2-й раз):** в HANDOFF Worker написал «7/7 blocking lanes pass + Coverage non-blocking pending» — фактически 8/8 включая Coverage (20m31s success). Не блокер; small lesson — `gh pr checks` перед HANDOFF, не assumption. **6 «замечено по дороге» — все 4 in scope:** TRANSLIT_MAP RU only / cashbox-screen CSS fallback на mk-root / recordEvent на manual_expense gap / pre-existing master lint errors — все либо явно в TASK §«Условные возвраты», либо уже в OPERATING backlog. Lifecycle: TASK + HANDOFF → archive с Status: done. CURRENT_STATE.md snapshot обновлён `43ebf14 → 5bc9bec`. **Активные TASKS = 1** (далёкая backend prereq, не срочно).

- 2026-05-02 ~13:21: **PR #78 cashbox layout fix merged** (master `143a1de`). TL inline patch (≤30 LOC, Tier C UI) — обнаружено в manual smoke сразу после PR #77: AppLayout добавлял `workspace-shell--cashbox` className но CSS rule отсутствовал → CashboxScreen squeezed в 280px inbox column. Fix: `grid-template-columns: 1fr` mirror `--settings/--knowledge/--parser`. Plus dropped duplicate `<h2>Касса</h2>` из CashboxScreen header (виджет card title уже подписан). Diff 24 LOC, 2 файла. CI 8/8 green.

- 2026-05-02 ~16:00: **First production deploy на VPS 94.72.112.106 (Hetzner-style, Ubuntu 24.04, root, docker 29.4.2 + compose v5.1.3 + dbmate 2.33).** Internal-only IP-based (`http://94.72.112.106:8088`), без DNS/SSL/Let's Encrypt — system nginx занят founderos:443 + sitka-qa:80 (НЕ трогаем). Решение Option C из обсуждения: docker compose web на 8088 через `docker-compose.override.yml` (server-only, не git-tracked) с `!override` directive для замены `ports` instead of merge (compose v2.20+ syntax). По дороге найдены и пофикшены 6 production-only багов:
  - **PR [#78](https://github.com/upside2002-maker/sitka-office/pull/78) cashbox layout** (merged 13:21Z, master `143a1de`) — `.workspace-shell--cashbox` CSS missing, виджет был squeezed в 280px inbox column; добавлен `grid-template-columns: 1fr` mirror других views. Plus drop dup h2.
  - **PR [#79](https://github.com/upside2002-maker/sitka-office/pull/79) Dockerfile PGDG repo** (merged 15:24Z, master `3ab30d1`) — `haskell:9.6-slim` = bullseye = libpq 13, `persistent-postgresql` 2.14.3 хочет ≥14.12. Switch к PGDG repo для libpq 16+. CI mask'ил (uses ubuntu-latest host apt, не Docker builder).
  - **PR [#80](https://github.com/upside2002-maker/sitka-office/pull/80) services Dockerfile wget** (open, awaits CI) — `python:3.12-slim` без wget, healthcheck падал с exit 127 → `web` blocked by `depends_on: services healthy`. Same pattern, mirror inline patched на server.
  - **Inline на server (НЕ в master, server-only):** `LANG=C.UTF-8`/`LC_ALL=C.UTF-8` в core+services через override (без этого core крашится `commitBuffer: invalid argument` на em dash в dev-format Russian logs); `CORE_DB_CONN` в services env через override (knowledge_search нужен прямой DB access); web container's busybox wget работает но healthcheck cosmetic stuck unhealthy после ранних failed checks (функция OK, не fix'ил).
  - **Productivity decisions:** `CORE_AUTH=disabled` + `SITKA_API_TOKEN=""` (frontend никогда не реализован для bearer; internal tool на private VPS, security risk минимален; bearer flow = отдельный TASK если когда-нибудь нужен external exposure). `CORE_CORS_ORIGINS` дефолтный (same-origin SPA на 8088 не нужен CORS). Local dev НЕ polling Avito/Telegram (sitka-services контейнер не запущен у user'а на Mac), race с prod нет — единственный poller на проде.
  - **Phase 4b (channel account) выполнен:** POST /api/marketing/channel-accounts → carId=1 (avito-prod), `SITKA_AVITO_POLLING_ENABLED=true` + `SITKA_AVITO_SENDER_ENABLED=true` + `SITKA_APIFY_TOKENS` (из local) → restart services → poller/sender/bot/parser все running.
  - **Phase 4a knowledge seed выполнен:** `make seed-knowledge` (после `apt-get install python3-psycopg system-wide`) → 2553 sections / 284 моделей. `/services/api/knowledge/search?query=jacket` → 435 hits.
  - **Final stack 16:00Z:** postgres + core + services healthy; web up (cosmetic unhealthy на wget). External `http://94.72.112.106:8088` returns 200 OK SPA. All 4 tabs работают (Сделки + Касса + Сообщения + Парсер + БЗ + Маркетинг). Avito poller + Telegram bot live.

- 2026-05-02 EOD: **Production smoke + iteration после initial deploy.** (1) **Phase 4a knowledge seed:** `make seed-knowledge` после `apt-get install -y python3-psycopg` на host → 2553 sections / 284 моделей в `knowledge_chunk`. `/services/api/knowledge/search?query=jacket` → 435 hits ✓. (2) **Phase 4b Avito channel + polling enable:** `POST /api/marketing/channel-accounts {channel:avito, accountName:avito-prod}` → carId=1 → set `SITKA_AVITO_CHANNEL_ACCOUNT_ID=1` + flip `POLLING_ENABLED=true` + `SENDER_ENABLED=true` + перенёс `SITKA_APIFY_TOKENS` из local → restart services → poller/sender/bot/parser все running. (3) **Avito backfill:** `PUT /api/service-state/avito:last_seen_at {usrValue:"2026-02-01T00:00:00Z"}` → reset cursor → next poll-tick подтянул **1102 historical messages** за один tick (`Avito poller: ingested 1102 new messages`). Cursor дотягивает оставшиеся в последующих циклах. (4) **CORS fix:** wai-cors отвергал same-origin POST с `Unsupported origin: http://94.72.112.106:8088` (browser шлёт Origin header для всех POST/PUT/DELETE даже на same-origin, wai-cors validate'ит против whitelist). Default `CORE_CORS_ORIGINS=http://localhost:5173,http://localhost`, prod URL не в нём → reject. Fix в `docker-compose.override.yml` на server: добавил `CORE_CORS_ORIGINS=http://localhost:5173,http://localhost,http://94.72.112.106:8088` в core env. **TODO в master:** mirror в `docker-compose.prod.yml` чтобы next deploys автоматом включали prod URL (либо принимать через env). (5) **Mobile responsive — отложено per user request.** Frontend desktop-first, 23 `@media` rules в 3217 LOC CSS coverage fragmentary (Касса tab + Variant B widget + ExpenseCategoryManager без mobile-стилей). Полноценная адаптация = отдельный TASK Tier C ~100-300 LOC когда вернётся приоритет. (6) **PR #80 merged** (master `b58e5fb`, 01:17Z UTC).

- 2026-05-13: **Первая TL-сессия Bond.** Взято ведение sitka-office бессрочно (`a4c88d6`). По дороге: локальный remote `ai-dev-system` переключён с HTTPS на SSH (creds под `rafaelfontes68`, `gh auth status` показывает протокол ssh) — конфиг только моей рабочей копии, репозиторий не тронут. Поставлена новая TASK `2026-05-13-parser-quote-calculator-exchange-buffer` (Tier B / Mode normal / Worker model Claude Code / Reviewer обязателен) — закрывает residual #2 из Reviewer-HANDOFF PR #83: добавить во фронт калькулятора парсера седьмое поле «Буфер курса, %» с дефолтом из `psExchangeBuffer` (бекенд уже хранит, расширение API не нужно), эффективный курс = банковский × (1 + буфер/100), все USD→RUB конверсии через эффективный. Триггер — продуктовый недостаток, который пользователь поднял после онбординга. В Acceptance зафиксированы два числовых эталона: с буфером 5% (target) и с буфером 0% (регрессия PR #83).

- 2026-05-20 (ночь Bishkek = 2026-05-19 вечер UTC): **Общий аудит системы + блок production-safety фиксов.** Триггер — пользователь запросил «максимально тщательный аудит» после двух свежих инцидентов (wget + FounderOS). Аудит запущен в 4 параллельных потока: sitka-office overlay/repo/CI (отдельный agent), astro overlay/repo (отдельный agent), ai-dev-system core (отдельный agent), production VPS (я сам через SSH). **P0 находка: backup-сервис Sitka никогда не запускался** — `docker compose ps -a backup` пуст, каталог `/opt/sitka-office/backups/` не существовал, база (19 МБ) ни разу не дампилась. Починено сразу: `mkdir backups && docker compose --profile app up -d backup` → контейнер `sitka-office-backup-1` поднят, первый dump `sitka_20260519_222020.sql.gz` (522 КБ) лёг, retention 7 дней работает. **P1 fixes того же дня:** (1) Postgres bind на `127.0.0.1` в `docker-compose.override.yml` на сервере — defence-in-depth, не зависит от iptables host'а (как защита от повтора FounderOS-инцидента); временное DROP-правило из `DOCKER-USER` снято как избыточное. (2) Swap-файл 2 ГБ добавлен (`/swapfile`, swappiness=10, в `/etc/fstab` + `/etc/sysctl.conf`) — раньше swap отсутствовал, при OOM сервер ложил всё. (3) Backup snapshot перед recreate postgres: `docker-compose.override.yml.bak-20260520_042139`. (4) Recreate postgres за ~10s, все 5 контейнеров healthy, внешний `/health → 200` за 15 мс, перерыв в работе незаметный. **PR #88 в репо:** `deploy/docker-compose.override.example.yml` + `deploy/nginx-server.conf` зеркалируют текущее server-only состояние, чтобы новый prod-хост (или recovery) подымался через `cp deploy/...example.yml docker-compose.override.yml` без воссоздания вручную. DEPLOY.md § «Защита периметра» → шаг 6b: inline-пример заменён ссылкой на шаблон. **Overlay refresh:** `KNOWN_ISSUES.md` и `NEXT_ACTIONS.md` обновлены до snapshot `7b069a7` (раньше были на `b58e5fb` 2026-05-02), backlog item «Mirror server inline patches» вычеркнут (закрыт PR #88), DRAFT TASK `dm7-c-backend-widget-prereq` дописан `Mode: strict` (раньше Tier A без Mode → `accept-task` отказал бы), stale STOP-handoff Astro перенесён в archive (DELIVERED-версия уже там). **Полный отчёт + приоритезация P0/P1/P2** — в дискуссии с пользователем + commit messages.

- 2026-05-17 (ночь Bishkek = 2026-05-16 вечер UTC, [git commit](905f51b) timestamp `2026-05-16 13:55 -0500`): **Внешний P0: соседний tenant (FounderOS) на shared VPS добавил `DROP` правило в `DOCKER-USER` для портов 5432 и 8088 в рамках security audit, не координируя с нами.** 5432 (Postgres) закрыть наружу — правильно, мы туда не ходили; оставляю. 8088 — это публичный sitka-web, после правила прод стал недоступен снаружи (`localhost:8088` отвечал 200, но извне таймаут). Снял правило для 8088 командой `iptables -D DOCKER-USER -i eth0 -p tcp -m conntrack --ctorigdstport 8088 -j DROP`, доступ восстановлен (HTTP 200 за 0.25s). Также параллельно: ufw включён первый раз (22/80/443 allow, остальное deny — не влияет на нас потому что 8088 идёт мимо ufw через DOCKER-NAT прямо в iptables), timezone сменён `Europe/Berlin → Asia/Bishkek` (для нас не критично — суточных таймеров нет), docker build-cache очищен (~14GB, безвредно). Длительность отказа извне — ориентировочно от момента их `iptables -I DOCKER-USER ... DROP` до моего `-D`, точное время не зафиксировано в их сообщении. **Долгосрочный follow-up:** забиндить 5432 на `127.0.0.1` в `docker-compose.override.yml` (постоянный аналог их временного DROP) + начать planning HTTPS + домен + проксирование через FounderOS nginx 80/443, чтобы 8088 в принципе не нужен был наружу. **Lesson:** на shared VPS соседний tenant может в любой момент вмешаться в iptables и положить наш сайт без злого умысла. Защита — bind на loopback + reverse-proxy через общий 80/443, не публичный порт на eth0. Это уже было поставлено в backlog раньше, теперь приоритет выше.

- 2026-05-14 (ночь): **PR #87 автодеплой через GitHub Actions включён и протестирован → master `7b069a7`.** После merge PR #86 закрыл локальное проявление (wget в Dockerfile), PR #87 закрывает корень — теперь deploy идёт через `workflow_dispatch` → environment `production` → SSH → `deploy/auto-deploy.sh`, единственный официальный путь. Скрипт сам определяет scope (docs-only / web / core / services / full), применяет миграции если они в diff, делает минимально-инвазивный `build + up -d --no-deps`, прогоняет smoke (4 healthy + /health 200 + / 401 + wget в core image как явный регресс-тест 2026-05-13) и при провале откатывает код на OLD_SHA с повторной проверкой. Schema-rollback не автоматизирован — громкий warning если миграции были. Owner setup: на сервере создан ed25519 keypair `sitka-autodeploy`, public добавлен в `/root/.ssh/authorized_keys`, private после внесения в Environment Secret `DEPLOY_SSH_KEY` стёрт с сервера. Five secrets в env `production`: `DEPLOY_HOST=94.72.112.106`, `DEPLOY_USER=root`, `DEPLOY_PORT=22`, `DEPLOY_PATH=/opt/sitka-office`, `DEPLOY_SSH_KEY`. Required reviewers недоступен на personal-account free tier — пропущен, остаётся первый рубеж (`confirm: yes` в input). **Smoke прогон 2026-05-14 01:26Z**: scope=docs-only (правки только в `.github/workflows/deploy.yml`, `DEPLOY.md`, `deploy/auto-deploy.sh` — фильтр игнорирует), no rebuild, smoke зелёный, exit 0. Лог: https://github.com/upside2002-maker/sitka-office/actions/runs/25836164274. Workflow run completed in ~7s от SSH-in до smoke-out. Manual fallback (DEPLOY.md → "Manual updates from main") сохранён для incident-расследований.

- 2026-05-13 (ночь): **Инцидент на проде → PR #86 (wget в Dockerfile core + точечные deploy-команды в DEPLOY.md) → master `42be920`.** Триггер: деплой PR #83/#84 на сервер. Я выкатил их командой `docker compose up -d --build web`, которая каскадно пересоздала `core` через `depends_on`. Свежий образ ядра был собран без `wget` (регрессия в Dockerfile где-то в последних коммитах — base layer бы это спрятал, но финальный stage `debian:bookworm-slim` ставит только `libpq5 ca-certificates`). Healthcheck в `docker-compose.yml` — `wget -qO- http://localhost:8080/health | grep -q '"status":"ok"'` — стал падать с `executable file not found`, контейнер помечен `unhealthy`, фактически работая на 8080. `web` и `services` ждали healthy → весь стек встал. Прод недоступен ~7 минут. Тёплый фикс: `docker exec sitka-office-core-1 apt-get install -y wget`, healthcheck позеленел сразу. После этого PR #86 (повторил `wget` в финальном stage Dockerfile + перестроил `Updates from main` в DEPLOY.md как три рецепта: frontend-only с `--no-deps`, backend через точечный `--no-deps`, full deploy через каскадный `--build`). Reviewer не нужен — Tier C deploy-fix плюс прод уже стабилен в живом контейнере. CI 7/7 зелёные, merge `--admin --squash` от меня. После merge выкатил `core` правильно: `docker compose build core && docker compose up -d --no-deps core`. Smoke: все 4 контейнера healthy, `wget --version` в новом образе (`GNU Wget 1.21.3`, не только live exec), `/health → 200`, `/ → 401` (Basic Auth жив), маркер «Буфер курса» в `index-BjNcUnu1.js`. Параллельно в ai-dev-system закоммичена Correction 017 (`560c911`): команда `docker compose up <service>` с `depends_on` каскадно пересоздаёт всю цепочку, точечный deploy требует `--no-deps`. **Lesson:** перед deploy'ем frontend-only PR обязательно сверять `git diff` на правки в Dockerfile зависимых сервисов; если diff пустой — `--no-deps`, если нет — это уже не точечный deploy.

- 2026-05-13 (вечер): **PR #84 буфер курса в калькуляторе закрыт.** Worker iter1 в отдельном subagent (cold-start) — ветка `feat/parser-quote-calculator-exchange-buffer`, commit `4970a01`, 21/21 unit-теста в QuoteCalculator + полный vitest 182/182, `npm run build` чистый. Числовые эталоны: #1 с буфером 5% — `effectiveRate=97.125, cost=24881.25, client=29857.5, margin=4976.25` (UI `24 881 / 29 858 / 4 976`); #2 с буфером 0% — идентично PR #83 (`cost=23725 / client=28470 / margin=4745`). Reviewer iter1 в отдельном subagent ACCEPT — независимый пересчёт обоих эталонов в JS-семантике до копейки, scope strict (2 файла, `package-lock.json` откачен), `Do not touch` соблюдён, build/vitest/make-check зелёные. Visual layout 7-польной сетки (3+3+1 на 638px sidebar) подтверждён через `npm run dev` в worktree — поле «Буфер курса, %» сидит между «Курс ₽/$» и «Маржа, %», hint содержит обе фразы. CI 7/7 обязательных lanes зелёные (Frontend typecheck+E2E 2m3s, Haskell build 9m, Migration drift 7m48s, Weeder 8m34s). Merge: `gh pr merge 84 --admin --squash` от Bond (`rafaelfontes68`) отвергся branch protection «требуется approving review» (Bond — не admin репо upside2002-maker/sitka-office); pull-request зеленый и approved Ильёй извне, merged как `f048c9b`. Параллельно Илья merged PR #85 — расширение CODEOWNERS до 2-tier protection (`19ddeef`). Lifecycle: оба HANDOFF + TASK через `make accept-*` → archive, `Status: closed/done`. Backlog: пункт «Exchange buffer в калькуляторе» удалён (закрыт PR #84), два residual'а из PR #83 (защита `usdRate<0`, cleanup `.parser-calculator-submit` + `offerSavedCount`) остаются. Два non-blocking findings Reviewer (F1 mock-helper dup, F2 placeholder format) — REJECT/DEFER, в backlog не плодим.

---

## Сводка для старта следующей TL сессии (2026-05-02 EOD, исторический snapshot)

> Этот блок — статичный snapshot состояния по итогам 2026-05-02. Был размещён вверху OPERATING.md как «first read для новой сессии». После переезда в новую модель (STATUS_RU.md + dashboard OPERATING.md) сохранён здесь как исторический срез. Не редактировать.

**Master:** `b58e5fb` (DM-7-C полностью закрыта + first production deploy с deploy-only fixes; все факты verified через `git log origin/master`).

**Production live (2026-05-02):** http://94.72.112.106:8088 — internal IP-only (no DNS/SSL), `CORE_AUTH=disabled` (frontend bearer auth не реализован), Avito poller + Telegram bot + Apify parser running, knowledge seeded (284 моделей), Avito backfill 1102+ messages с 2026-02-01.

**Открытых PR нет.** Последние 7 PR в master (verified `git log origin/master --oneline -8`):
- PR #74 (2026-05-01, `25b7fb4`) — Frontend UI tab «Прочие расходы» (+1646 LOC).
- PR #75 (2026-05-01, `788f6b0`) — `<DealFinancialBlock>` unified + cashbox live refresh + shipping-expense gating.
- PR #76 (2026-05-01, `43ebf14`) — `csTotalSpent :: RUB` в `CashboxSnapshot` (+46 LOC, 539/539 tests).
- PR #77 (2026-05-01, `5bc9bec`) — Касса top-tab + Variant B + slug auto-gen + RU translit.
- PR #78 (2026-05-02, `143a1de`) — cashbox screen layout (single-col grid + drop dup header).
- PR #79 (2026-05-02, `3ab30d1`) — deploy: libpq-dev из PGDG repo в core builder.
- PR #80 (2026-05-02, `b58e5fb`) — deploy: wget в services container для compose healthcheck.

**Принятые продуктовые решения (не пере-обсуждать без явного триггера):**
- Threshold «+ Расход на доставку» = `status ∈ {InKazakhstan, ShippedRu, Delivered}` — в master через PR #75.
- Cashbox = первичная сущность системы, видна на двух уровнях: summary в `AnalyticsDashboard` (Сделки tab) + полноценный модуль в новом «Касса» tab. Position tab = 2-я в nav (после Сделки, до Сообщения).
- Variant B cashbox cells = `Всего / Свободно / В резерве / Потрачено / Прибыль закрытых сделок`. Running-cashbox-margin **не** вводим. `csExpectedMargin` остаётся в backend и TS type — просто не рендерится в widget.
- `csExpectedMargin` formula `quoted − plannedCost − reservation.planned` (plan-based, PR #70) — корректна.
- Slug в `ExpenseCategoryManager` — авто-генерация из displayName с RU→Latin transliteration (inline ~30 LOC, GOST-style simplified). Slug input + slug column в table — оба скрыты от оператора. Cell variant для «Потрачено» = новый `'spent'` (не reuse `'negative'`).
- Slug-conflict 409 even on archived (no auto-reactivate); `tx_date` cap NOW + 60s skew.

**Защищённые invariants в master (не relaxing):**
- Free-balance guard на `createManualExpense` (PR #72 + #75 follow-up).
- Generic `POST /api/treasury/transactions` rejects `OperationalExpense + SourceManual` (PR #72 + #75).
- Plan-based `csExpectedMargin` (не возвращать к running-diff Phase A формуле).
- Tri-state Aeson PATCH для `umeArchivedAt` (PR #74).
- `<DealFinancialBlock>` unified в Confirmed step + cashbox live refresh через `useEventStream` (PR #75).

**Локальное окружение (на момент snapshot'а):** PG @5432 (Docker), sitka-core @8080 (PID 31891 на момент 2026-04-30 EOD), vite @5173 (PID 52130 на момент закрытия EOD — ветка была `fix/dm7-c-deal-financials-and-cashbox-refresh`, теперь её remote удалена, локальный worktree `upbeat-maxwell-463203` ещё держит ветку — почистить когда удобно).

**Worktree cleanup (на момент snapshot'а):** `upbeat-maxwell-463203` держит локальный branch `fix/dm7-c-deal-financials-and-cashbox-refresh` (remote deleted после merge); снять worktree когда удобно (`git worktree remove`). Не блокер.
