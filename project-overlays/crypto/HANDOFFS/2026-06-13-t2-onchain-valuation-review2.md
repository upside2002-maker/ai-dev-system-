# Контрольное ревью Т-2 (доработка) — сведённый вердикт Admin'а

**Дата:** 2026-06-13
**Метод:** контрольное многолинзовое ревью доработки, ветка feat/t-2-onchain.
**Вердикт: ACCEPT.**

## Важно: первый прогон дал ЛОЖНЫЙ REJECT

Первый контрольный прогон вернул 4 «критических» — все на единственной
ложной посылке «доработки нет на ветке». Причина: ревьюер работал на
УСТАРЕВШЕМ клоне (состояние до доработки: старая граница, 27 тестов).
Admin проверил руками — доработка на месте (нужный коммит на вершине,
граница `>=` в обеих строках, 36 тестов, единый механизм деградации).
Ложный вердикт НЕ принят; стоялые клоны вычищены; ревью перезапущено с
sanity-gate (проверка HEAD и маркеров доработки ПЕРЕД ревью).

Урок: красный итог ревью ≠ правда; факт сверяется по единицам (память
feedback-research-runs-limit-discipline). Гонки/устаревшие клоны роняют
агентов независимо от вердикта.

## Перезапуск — ACCEPT

Все 3 линзы прошли sanity-gate (HEAD совпал, маркеры доработки совпали,
make check зелёный) и не нашли дыр:
- **Б1 граница детектора:** `>=` в обеих ветках; значение ровно на пороге
  выпадает → семья молчит; граничные тесты честные.
- **Б2 наблюдаемость (рецидив Т-1 закрыт):** каждый класс сбоя → явный лог
  + пометка source:<имя> в data_quality + critical_ok=false; молчаливых
  except/return {} нет; единый DegradeLog переиспользован.
- **Б3 документирование:** абсолютный смысл max_rel_jump при нулевой базе
  зафиксирован в коде и config/snapshot.yaml.
- Периметр: schemas/core/paper не тронуты; снимок валиден; кросс-хэш цел;
  секретов нет; прежние тесты целы.

## Итог

Т-2 принят. Вместе с Т-4a — две чистые приёмки → слияние обеих веток в
master (файлы не пересекаются). Предохранитель ядра (сбой → семья молчит →
critical_ok=false → ядро observe_only) усилен и подтверждён.
