# TASK: t2-onchain-valuation

- Status: review
- Ready: yes
- Date: 2026-06-13
- Project: crypto
- Layer: services
- Risk tier: B
- Owner: Project Tech Lead
- Created by: upside2002@gmail.com
- Worker model: Claude Code (Opus) — параллельная сессия Owner'а
- Worker branch: feat/t-2-onchain (изолировано от Т-4a по решению Owner: развести)
- Mode: normal
- Critical approved by: (нет)
- Reviewer: (обязателен — независимая сессия)

## Problem

Он-чейн чтение: заполнение КРИТИЧНОЙ семьи valuation в снимке дня. Без неё
ядро вечно в observe_only; с ней `critical_ok` переключается на true, когда
семья жива — ядро получает право на денежные действия. Это самый
ответственный кусок данных: от него зависит право ядра на деньги.
**Работа УЖЕ выполнена в параллельной сессии Owner'а** (коммит ca4af91,
ветка feat/t-2-onchain); этот TASK оформляет её ретроактивно для НЕЗАВИСИМОГО
ревью в общем конвейере (по решению Owner'а 13.06 «развести, ревьюить
раздельно»). Архитектура §3.3 (детектор сбоев), §9.2 (метрики valuation),
§3.1 (декларации поставщиков), дыра Д-1 (история он-чейн).

## Files (фактически в ca4af91, ветка feat/t-2-onchain)

- new: platform/cpds_platform/collectors/onchain.py — браузерное чтение +
  детектор сбоев (пусто/вне-диапазона/скачок/нечитаемо → семья молчит).
- new: platform/tests/test_onchain.py + 4 фикстуры (валидная страница,
  битая вёрстка, анти-бот заглушка, ряд Coin Metrics).
- modify: config/snapshot.yaml, snapshot.py, families.py, config.py, cli.py,
  vendors.yaml, test_vendors.py.
- schemas/, core/, paper/ — НЕ тронуты (заявлено автором; проверить ревью).

## Контракт (проверить ревью)

- Семья valuation заполняется метриками §9.2 (MVRV-Z, NUPL, отношение к
  реализованной цене); зоны по квантилям собственной истории; окна/пороги
  в конфиге, НЕ в коде (калибровка Г-0/Т-7 позже).
- `critical_ok` → true ТОЛЬКО когда семья жива; при сбое источника
  (детектор §3.3) семья молчит → `critical_ok` снова false (предохранитель
  ядра цел: нет он-чейн → ядро только наблюдает).
- Чтение браузерное (как farside, честная деградация при анти-боте) +
  бесплатные ряды Coin Metrics для квантилей. В тестах сети нет (фикстуры).
- Схему market_snapshot НЕ трогать (заполняется существующий контракт).

## Do not touch (для возможной доработки)

- schemas/, core/ (Haskell) — не трогать.
- Зона Т-4a (paper/, config/paper.yaml) — не трогать (другая колея).
- Никаких ключей/секретов в репо. Никаких Double/float в денежных/зонных
  расчётах, где платформа уже даёт фикс. точность.

## Acceptance (ревью проверяет на ветке feat/t-2-onchain, изолированно)

- [ ] `make check` зелёный с чистого клона ветки feat/t-2-onchain.
- [ ] Семья valuation заполняется из фикстуры валидной страницы; зоны
  считаются; `critical_ok=true`, когда семья жива (тест).
- [ ] ПРЕДОХРАНИТЕЛЬ (главное): сбой источника (битая вёрстка / анти-бот /
  вне-диапазона / скачок) → семья молчит → `critical_ok=false` → снимок
  собирается, ядро на нём отвечает observe_only (регресс §9.5 инв.3 цел).
- [ ] Зоны по квантилям из КОНФИГА, не зашиты в код (тест/grep).
- [ ] Схема market_snapshot не тронута; снимок валиден; кросс-хэш снимка
  Python==Haskell цел (если затронут хэш-путь).
- [ ] Секретов/ключей в репо нет; периметр: schemas/core/paper не тронуты.

## Context

- Архитектура: `ai-dev-system/docs/CRYPTOBOT_ARCHITECTURE_2026-06-12.md`
  §3.1–§3.5 (декларации, снимок, качество, дыра Д-1 история он-чейн), §9.2
  (метрики семей), §3.3 (детектор сбоев → семья молчит).
- Принятый Т-1: контур данных (этот кусок достраивает критичную семью).
- Принятое ядро Т-3: critical_ok=true даёт ему право на деньги — поэтому
  предохранитель (сбой → critical_ok=false) проверять особенно строго.
- Дыра Д-1 (KNOWN): глубокой истории он-чейн нет; квантили на бесплатных
  рядах + накопление вперёд — учесть как ограничение, не блокер.
- Слияние feat/t-2-onchain и feat/t-4a-fix в master — после ДВУХ чистых
  приёмок (решение Owner'а «развести»); файлы не пересекаются (проверено).
