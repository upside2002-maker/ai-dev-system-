# Журнал — astro — 2026-06

## Профориентация (метод Дарагана) — эпик

- **Phase 0** (method-fidelity proof, READ-ONLY) — **WORKER COMPLETE** 2026-06-07.
  TASK `2026-06-06-proforientation-phase0-method-proof.md`; HANDOFF
  `HANDOFFS/2026-06-06-worker-to-tl-proforientation-phase0-method-proof.md`.
  STOP-gate **PASS**. Продуктового кода нет.
  - **Golden anchor PASS:** механический прогон алгоритма на «Карта 11. Банкир»
    (кн. 300–307) воспроизводит таблицу Дарагана (кн. 302) до буквы — все 4 управителя
    домов (упр.X=Jupiter, упр.II=Mars, упр.VI=Moon, упр.VIII=Venus), Pluto НЕ сигнификатор
    ни одного из 4 домов (его ключевой тезис), упр.солн.знака=Venus, упр.Асц=Uranus+Saturn.
    Вывод (Jupiter упр.X в VIII → финансовый менеджер/банкир) совпал с разбором Дарагана и
    реальной судьбой натива. Фиделити доказана на worked example, не только на математике.
  - **Дорифорий/Возничий — определение ЗАКРЫТО golden-эталоном:** в книге термины НЕ
    определены (используются только в алгоритме кн.294 + таблице Банкира). Таблица Банкира
    (Sun 17° Libra; Дорифорий=Jupiter −3°, Возничий=Neptune +15°) эмпирически фиксирует
    правило: **Дорифорий = ближайшая планета ПЕРЕД Солнцем по эклиптической долготе
    (ориентальная), Возничий = ближайшая ПОСЛЕ (оккцидентальная)**, по всем 10 планетам,
    без орб-капа. Re-derive совпал с Дараганом по обеим планетам. Флаг Марине: классический
    строгий дорифорий иной — берём простое правило Глоба-школы/Банкира.
  - **Фигуры Джонса — НЕ определены в книге** (Луна=«ручка Пращи» в Банкире — согласуется
    со стандартной доктриной Jones: bowl/bundle + singleton-handle). Определение по внешней
    доктрине, флаг.
  - **Благоприятные звёзды — OPEN: списка в книге НЕТ.** Дараган относит благие звёзды к
    *элекциям* (пример «Регул на Юпитере / Alphabet Inc»), называет только «королевские
    звёзды» (Regulus) без перечня; для призвания — «на одной из астрологически значимых
    звёзд» без списка. Back-matter рекомендует Robson/Hofman. Phase A: либо без стадии-2(з),
    либо курированный список с одобрения Марины (НЕ выдумывать — запрет ТЗ).
  - **Сигнификаторы (planet→деятельность, кн.69–79; дома кн.93–97)** выписаны со ссылками
    на страницы — данные для `packages/rulesets/vocation/` (стадия 5, правит Марина).
  - **Ручной прогон Марины (person 4):** топ-2–3 = **Jupiter (упр.X в II, Gemini; Σ7)** —
    оптимум по правилу «связь с X»; **Venus (упр.II, в I, Pisces exalt, Дорифорий; Σ6)** —
    сильнейший талант; **Sun (в I, Aries exalt; Σ5)**. Синтез: экспертная частная практика
    (консультант/преподаватель/методолог/автор) + венерианский талант + харизматичное Солнце.
    Совпадает с её реальной ролью (астролог-консультант). Призвание НЕ навязывается (Jupiter
    в изгнании, не Солнце/упр.Асц — DoD соблюдён). Moon, Neptune исключены (пустая колонка
    способностей → «нежелательно»).
  - **Reuse-vs-build (по чтению ядра):** REUSE — `planetInHouse`, `interceptedSigns`,
    `rulersOfSign` (dual modern+traditional), `isDomicile`/`isExaltation`,
    `findAspects`/`filterHarmonious`, `ascendantZodiacPosition`, `isAboveHorizon`.
    Паттерн house-ruler уже есть внутри `Directions.housesForPlanet` (cusp+intercepted) →
    тонкий BUILD `rulersOfHouse`. BUILD (отсутствует в ядре): **взаимная рецепция**
    (grep 0 hits; формула из Dignities), **дорифорий/возничий** (signed-sep-from-Sun +
    nearest-each-side), **фигуры Джонса** (Stellium per-house НЕ годится — нужен 360°-shape),
    **fixed-star список**, агрегирующий `Domain.Vocation` (таблица+ранжирование). Соединение
    = Neutral в ядре → «соединение ИЛИ гармоничный» кодировать как (Conjunction ∪ Harmonious).
  - **Открытые вопросы для TL/Марины:** (1) список благих звёзд; (2) подтверждение правила
    дорифория; (3) кейсы Jones + handle-only; (4) quincunx в 1в/2д; (5) ещё 1–2 reference-
    разбора Марины для golden Phase A помимо Банкира+Марины.

- **Phase A1** (ядро `Domain.Vocation` + факторы + golden/unit-тесты; БЕЗ схемы/Bridge/Main)
  — **WORKER COMPLETE, ready-for-review** 2026-06-07. TASK
  `2026-06-06-proforientation-phase-a1-core-vocation.md`; HANDOFF
  `HANDOFFS/2026-06-06-worker-to-tl-proforientation-phase-a1-core-vocation.md`.
  Product **committed 7dc313e** (baseline 51f1e57); **Tier A → независимый Reviewer
  REQUIRED** (Correction 021; спускает TL). 0 STOP.
  - **Новый pure-Core `Domain.Vocation`** (+ под-модули `.Reception` / `.Satellites` /
    `.JonesPattern`) — стадии 1–4 алгоритма Дарагана. Вся значимость-логика в ядре
    (bright line #7 / AST-ARCH-1). Выход = таблица факторов (10 планет) + ранжированные
    топ-2–3 «планета+дом» с машинными ключами факторов. БЕЗ толкования (стадия 5 = Phase B),
    БЕЗ схемы/Bridge/Main (Phase A2), БЕЗ Python/PDF/UI (Phase B).
  - **Достроены факторы** (в ядре отсутствовали): взаимная рецепция (симметрична,
    обитель/экзальтация, смешанная допустима; дедуп по партнёру → один фактор на пару по
    лучшей денежно-домовой роли); Дорифорий/Возничий (знаковая сепарация от Солнца, ближайший
    до/после, все 10 планет, без орб-капа); фигуры Джонса bucket/sling (360°-shape;
    `Stellium` per-house НЕ годится; ручка-планета кормит 2ж); `rulersOfHouse` (cusp+intercepted,
    reuse паттерна `Directions.housesForPlanet`); `rulerOfSolarSign`.
  - **«Соединение ИЛИ гармоничный» закодировано явно** (Conjunction ∪ Harmonious; Conjunction
    = Neutral в ядре). Quincunx НЕ в гармоничных (locked). NO wildcard над Planet (Correction 002).
  - **Golden «Банкир» воспроизводит таблицу Дарагана (кн.302) по всем перечисленным ячейкам**
    + вывод: 4 управителя (X=Jupiter, II=Mars, VI=Moon, VIII=Venus), Pluto-НЕ-сигнификатор
    (оба столбца пусты), упр.солн.знака=Venus, упр.Асц=Uranus+Saturn, **Дорифорий=Jupiter /
    Возничий=Neptune**, элевация=Venus, ручка Джонса=Moon; топ-комбо **Jupiter упр.X в VIII**
    (банкир/фин.менеджер), Jupiter > Луна. Фиделити доказана на published worked example.
  - **2 задокументированных НЕ-нагрузочных расхождения (флаг, НЕ подгонка):** (1) Uranus
    «секстиль с Солнцем» Дарагана = на самом деле квинконс 149° (Uranus 16 Pisces vs Sun 17
    Libra) — locked-решение исключает квинконс → движок не воспроизводит; Uranus с пустой
    реализацией всё равно отфильтрован, на вывод НЕ влияет. (2) Saturn в моём fixture даёт 2
    лишних реализационных секстиля к Mars/Jupiter (кластер Весов ~6° делает Sun-секстиль
    геометрически неотделимым) — по locked-правилу движок их включает; на вывод НЕ влияет
    (Jupiter всё равно #1). Оба — присущи воспроизведению ручного разбора единым механическим
    правилом аспектов.
  - **Golden «Marina» (person 4):** топ-3 = **Jupiter (упр.X в II; Σ7) / Venus (упр.II в I,
    Дорифорий; Σ6) / Sun (в I; Σ5)**; Moon & Neptune исключены (пустые способности) — точно
    как ручной прогон Phase 0.
  - **Unit:** рецепция (dom×dom, mixed dom×exalt, симметрия, отрицательные), дорифорий/возничий
    (синтетика + wrap через 0°), Джонс bucket/sling/none (синтетика), house-rulers, детерминизм
    + порядок Sun..Pluto.
  - **Verify:** `cabal build` чисто (0 warnings); `cabal test` **334/0** (baseline 279 + 55
    новых). **Solar + returns golden бит-в-бит** (`git diff test/golden/` пуст). Проверено на
    committed-state (Correction 008). `git status` чисто для intended (7 Core-файлов).
  - **Edge-флаги Марине/Reviewer:** Jones figure-type (Bucket vs Sling) doctrine-loose — но
    ручка-ПЛАНЕТА (Moon) совпадает с Дараганом, что и важно для 2ж; дорифорий = простое
    правило (не строгий эллинистический); рецепция с внешними планетами — formula по
    domicile/exaltation (Банкир-fixture следует таблице Дарагана: Neptune в поздних Весах/VIII).

- **Phase A2** (schema-change-gate bright line #8: standalone `vocation` workflow — контракт
  + Bridge DTO + Main dispatch + golden + Python contract + TS; + F1-cleanup) — **WORKER
  COMPLETE (self-submit, ждёт независимого Reviewer'а — Tier A, Correction 021)** 2026-06-07.
  TASK `2026-06-06-proforientation-phase-a2-schema-gate.md`; HANDOFF
  `HANDOFFS/2026-06-06-worker-to-tl-proforientation-phase-a2-schema-gate.md`.
  Продукт **committed `b7cf858`** (astro main; baseline A1 `7dc313e`).
  - **ВСЕ 5 артефактов + F1 — в ОДНОМ атомарном коммите** (bright line #8), по образцу
    возвратов Phase C (`37db1c2`): (1) `packages/contracts/vocation-{input,output}.schema.json`
    greenfield (Draft-2020-12, `additionalProperties:false`); (2) `src/Bridge/Vocation.hs`
    (`VocationResolvedInput` FromJSON + `VocationComputedFacts` ToJSON + `runVocation`) +
    `app/Main.hs` ветка `"vocation"` + `.cabal`; (3) `test/golden/vocation-sample.{input,
    expected}.json` + `Test.GoldenVocation` (parse→runVocation→expected + input roundtrip);
    (4) `packages/test-fixtures/vocation-sample.json` + `test_contracts.py` (3 теста);
    (5) `apps/web-react/src/types.ts` (`VocationComputedFacts` + sub-типы).
  - **Профориентация — статичный натальный анализ:** вход = `natal_positions` + `house_cusps`
    (Placidus, 12 куспидов + Asc + MC; `dsc`/`ic` опциональны, движок выводит Asc+180/MC+180)
    + `meta`. **БЕЗ `transit_samples`, БЕЗ `reference_jd`** (в отличие от solar/returns). Выход
    = `top_combinations` (`{planet, connected_houses, factor_keys, rank, score}`, ≤3) +
    `factor_table` (полная таблица 10 планет, машинные ключи факторов стадий 1–2). **БЕЗ
    текста толкования** (стадия 5 = Phase B).
  - **Golden = «Карта 11. Банкир»** (тот же чарт, что в A1): топ-комбо ранг 1 = **Jupiter
    упр.X в VIII** (Σ7), затем Saturn (Σ6), Venus (Σ4); Moon/Neptune/Uranus/Pluto вне комбо,
    но присутствуют строками factor_table. Expected-фикстура сгенерирована ИЗ вывода CLI, так
    что schema↔Bridge верны by construction.
  - **Reuse не дубль (Correction 001):** `Bridge.Vocation` импортирует `BridgePlanetPosition`
    + `BridgeMeta` из `Bridge.Solar`; строки `factor_table` переиспользуют `PlanetFactors`
    ToJSON. **Ноль правок `Bridge.Solar`** (house-cusps INPUT — новый смысл; output-only
    `BridgePlacidusResult` для входа не годится — несёт `"system"`-const).
  - **F1 (находка ревью A1):** удалена мёртвая `planetsInHouse` из `Domain/Vocation.hs`
    (−5 строк, единственная правка Domain.Vocation); golden бит-в-бит, A1 VocationSpec зелёный.
  - **Verify (на committed-state, Correction 008):** `cabal build` чисто (0 warnings);
    `cabal test` **338/0** (baseline 334 + 4 vocation golden); **solar/returns схемы+golden
    бит-в-бит** (`git diff 7dc313e..b7cf858` по solar-*/returns-* пуст); `runSolar`/`runReturns`/
    dispatch живые (solar→`"solar"`, returns→`"returns"`, vocation→`"vocation"`, bogus→exit 2);
    `pytest test_contracts` **11/11**; `tsc --noEmit` чисто. `git status` чисто для intended
    (13 файлов; pre-existing `.claude/launch.json`/`_marina-deliverables`/`marketing-site`/
    `api-python/data` НЕ тронуты). **0 STOP triggers.**
