# Worker → TL — Saturn glyph fix iter-6

- Status: closed

**Date:** 2026-05-09
**Tier:** B normal
**Mode:** normal
**Commit:** `bbc7a82` (parent `57669b6`)

## Scope

Single-fix follow-up: Saturn-only path replacement in
`HAND_CRAFTED_OVERRIDES`. Iters 1-5 all rendered the bottom as a simple
quarter-circle hook ("j"-tail). Owner-supplied reference shows the
canonical bottom is a LARGE near-closed loop (script "5" descender),
and the top is a small Latin cross (crossbar ~4 units), not a wide
horizontal bar.

## Iterations needed

**1.** First-attempt path matched the owner's reference description
on the first standalone render — no parameter sweeps, no fallback.

## Final approach

Hand-crafted SVG path (no Wikipedia fallback needed).

## Final path string

```
M 0 -11 L 0 2 M -2 -9 L 2 -9 M 0 2 Q 8 2 8 7 Q 8 12 3 11 Q -2 10 -1 6
```

Components:
- `M 0 -11 L 0 2` — full vertical (cross top + stem) y=-11..2.
- `M -2 -9 L 2 -9` — small Latin crossbar at y=-9, width 4.
- `M 0 2 Q 8 2 8 7 Q 8 12 3 11 Q -2 10 -1 6` — large near-closed
  bottom loop (3 chained quadratics): out-and-down to (8,7), down-and-
  back-left to bottom-most (3,11), up-and-left ending inside loop near
  (-1,6).

## Artifacts

- **Standalone self-inspection PNG:** `/tmp/saturn-iter6.png`
  (small cross at top, vertical stem, prominent looped sickle below —
  matches reference).
- **Full-PDF render (Наталья 2025):**
  `/tmp/astro-natalya-after-glyph-swap-iter6.pdf` (121.5 KB).
- **Page-1 PNG (zoom 3200):** `/tmp/astro-iter6-p1-big.png` — Saturn
  visible at IX house cluster (top-right) at `1°25' R`, glyph reads
  recognizably as canonical ♄ in solar+natal wheel context.

## Tests

- `pytest` from `services/api-python/`: **70/70 passed** (8.64s).
- No fixtures changed, no schema changes.

## Files changed

- `services/api-python/scripts/build_wheel_glyphs.py` —
  `HAND_CRAFTED_OVERRIDES["Saturn"]` path + comment block.
- `services/api-python/app/pdf/wheel_glyphs.py` — regenerated by
  build script (single-line `_PATH` for Saturn).

## Out of scope confirmed

- No other glyphs touched.
- No `wheel.py` changes.
- Cardinal labels, axes, viewBox, aspect-removal — all from iter-4,
  unchanged.
- Stroke-width 2.4 black — unchanged.

## Tool budget

8 / 15 tool uses. No fallback path needed.
