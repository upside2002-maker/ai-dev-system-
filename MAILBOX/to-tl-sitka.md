# Почтовый ящик: для TL Sitka

От Admin (по поручению Owner'а). Формат — `MAILBOX/README.md`.

Прочитанные сообщения переезжают в `archive/<YYYY-MM>.md`.

---

## 2026-05-25 (поздняя ночь) — AVITO от Owner через Admin, для TL Sitka

**Что:** по твоему вопросу №3 про `sitka-services/vendor/avito_parser/` — Owner выбрал вариант **«сначала проведём аудит avito_parser отдельно»**. Делается **после** закрытия inventory-форка (то есть после TASK D из текущей декомпозиции), не параллельно.

Формат аудита — точно такой же как для inventory: 7 разделов (резюме / карта / соответствие архитектуре / проблемы по категориям / находки с координатами / приоритизированный backlog / рекомендация по работе дальше). Адресат — Owner. Сохранить в `project-overlays/sitka-office/research/avito-parser-architecture-audit-YYYY-MM-DD.md` рядом с inventory-audit.

После получения аудита Owner так же примет решение форкать / vendor-patch'и / оставлять.

**Срочность:** не сейчас. Внеси в свой backlog как «next-after-fork», и стартуй только когда inventory-форк дойдёт до TASK D (finish-line четырёхзадачной декомпозиции).

---

## 2026-05-28 (поздний вечер) — РЕШЕНИЕ Owner'а по AVITO: Option 1 — от Admin

**Решение принято.** Owner выбрал **Option 1** (минимальный, рекомендованный твоим же §7) из трёх вариантов в твоей записке от 2026-05-28 (вечер). Рекомендация «оставить vendored» принята — форк (Option 3) закрыт окончательно.

**Что делать (один маленький PR, стандартный lifecycle):**

1. **Завести `vendor/avito_parser/UPSTREAM.md`** (P2-1): когда импортировано, с какого upstream-commit'а, dead-list (что не используется), наши патчи если были. Чтобы следующая сессия не реверс-инжинирила vendor с нуля.
2. **Подключить vendor's `test_market.py` к CI** (P1-2): ~3-5 LOC в `.github/workflows/ci.yml`. Цель — vendor становится «trusted **and** verified»: случайный edit ловится в CI, а не на проде.

Tier на твоё усмотрение (вероятно C, но трогает CI — реши сам). Ориентир из твоей оценки ~30 минут. Reviewer по твоему judgment.

**Чего НЕ делаем сейчас (Option 2 не выбран):** sync rotation markers (F-DUP-1) и `force_refresh` в `/api/avito/search` — НЕ в этом заходе. **НО F-DUP-1 не теряем:** занеси его в backlog отдельным **P1**-пунктом с явной пометкой «риск вырос после TASK C — Amazon работает → больше Apify-вызовов → выше шанс 429, а Amazon/eBay-runner НЕ ротирует токен при rate-limit». Если когда-нибудь оператор словит пустую выдачу под нагрузкой — первый подозреваемый там.

**После merge:** заархивируй записку про AVITO от 2026-05-25 (она ждала именно этого решения Option 1/2/3) → `archive/2026-05.md`, Status: done. Эту записку тоже забери в archive после прочтения. Доклад в `to-admin.md` после деплоя.

---

## 2026-06-02 — Доработка inventory_parser: ТЗ готово, в работу — от Admin

**Что:** Owner собрал развёрнутое ТЗ на доработку `inventory_parser` (real-time price/stock lookup под Sitka). Admin вычистил область до **только Sitka Gear** (хоккей/Bauer и прочие неотносящиеся бренды убраны) и сохранил:
`/Users/ilya/Projects/sitka-office/docs/PARSER_IMPROVEMENT_TZ.md`

**Боль (зачем):** парсер врёт о наличии и теряет результаты. Два корня:
- падение одного адаптера роняет весь прогон (теряются результаты остальных магазинов);
- сток отдаётся на уровне товара, а не конкретного variant (color×size) → «есть на Rogers $99», а нужного размера/цвета там нет или backorder.

**Приоритет (строго):**
1. **P0-1** — failsafe orchestrator (один сломанный адаптер не валит весь run). Первым.
2. **P0-2** — variant-level stock (сердце ТЗ; меняет модель `OfferCandidate`). Главная ценность.
3. **P0-3** — убрать hardcoded `/srv/sitka-qa/` пути (env + дефолт в `~/.cache`).

Дальше — P1 по списку. **P3 не трогать, пока P0 не закрыт.**

**Процесс:**
- Декомпозируй на отдельные TASK'и (минимум P0-1 / P0-2 / P0-3), не один мегатаск.
- DoD прописан в самом ТЗ (failing test → fix → live smoke на одном из 4 P0-2 кейсов → UPSTREAM.md/CHANGELOG). 7 запросов в конце ТЗ — это regression-suite.
- `inventory_parser` — **наш форк** (fork & own, серия A→D). Значит это «Local fixes», не «Shared bumps»; помни про CODEOWNERS-gate на `app/inventory/` shared core (TASK D).
- Tier/Mode на твой judgment, но: P0-2 меняет контракт выдачи и модель данных → ориентир Tier B (возможно B+), **Reviewer на P0-2 желателен**. P0-1/P0-3 контейнернее.

**Очередность с Avito Option 1:** та работа маленькая (~30 мин, UPSTREAM.md + CI-hookup) и уже решена — закрой её первой. Парсер — следующий крупный блок.

Доклад по ходу в `to-admin.md`.

---
